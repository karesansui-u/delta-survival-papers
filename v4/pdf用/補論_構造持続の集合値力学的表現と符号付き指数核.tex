\documentclass[12pt,a4paper]{article}
\input{survival_whitepaper_preamble.tex}

\begin{document}

\PaperTitleBlock
  {構造持続の集合値力学的表現と符号付き指数核}
  {— 収縮モードの拡張としての repair・learning・rollback —}
  {2026年5月29日}

\begin{abstract}
本補論の目的は、これまで主に収縮モードとして定式化してきた構造持続理論を、修復作用を含む集合値時間発展の表現へ拡張するための最小骨格を与えることである。現行の最小理論では、制約追加モデル

\[
V_{i} = V_{i-1} \cap C_{i}
\]

のもとで、生存可能状態集合の逐次縮小から指数型

\[
m(V_{n}) = m(V_0) e^{-L_{n}}
\]

が導かれる。しかし現実の系では、学習・修復・冗長化・外部支援・ロールバックのように、生存可能領域を再拡大しうる作用が重要になる。本補論では、各段階を収縮作用と修復作用の合成として与え、段階損失量と修復量を同じ対数尺度で定義することで、累積純損失量

\[
B_{n} = L_{n} - R_{n}^{\mathrm{rec}}
\]

に対する指数核

\[
m(V_{n}) = m(V_0) e^{-B_{n}}
\]

を与える。これにより、収縮モードは構造持続の集合値力学的表現の特例として回収される。
\end{abstract}

\section{はじめに}\label{ux306fux3058ux3081ux306b}

最小理論が直接に扱ってきたのは、制約追加が支配的であり、生存可能状態集合が段階ごとに縮小していく局面である。この場合、各段階段階損失を縮小率の対数比として定義すれば、残存量の指数表現は望遠鏡積として恒等式になる。しかし、同じ分冊群のなかでも、学習、修復、外部支援、ロールバックのような再拡大過程が重要であることはすでに認められている。したがって、収縮モードを理論の唯一の形とみなすより、それを構造持続の集合値力学的表現の一つの特例として位置づける方が自然である。

本補論の狙いは、まさにこの位置づけ直しにある。以下、この抽象表現を「構造持続の集合値力学的表現」と呼ぶ。ここで与えるのは、資源項まで含めた完成理論ではない。まずは、生存可能領域の質量に関する指数核が、収縮と修復をまとめた符号付き純損失量に対しても保存されることを示す最小骨格である。

\section{構造持続の集合値力学的表現}\label{ux69cbux9020ux6301ux7d9aux306eux96c6ux5408ux5024ux529bux5b66ux7684ux8868ux73fe}

系 \(X\) を固定し、初期の生存可能状態集合を

\[
V_0 \subseteq X
\]

とする。比較に用いる質量モデルを \(m\) とし、各可測集合 \(A \subseteq X\) に対して非負値 \(m(A)\) を与えるものとする。ここで \(m\) は、少なくとも単調性

\[
A \subseteq B \Longrightarrow m(A) \le m(B)
\]

を満たすと仮定する。

各時刻 \(t\) における更新は、収縮作用

\[
K_{t} : \mathcal P(X) \to \mathcal P(X)
\]

と、修復作用

\[
R_{t} : \mathcal P(X) \to \mathcal P(X)
\]

の合成として与える。ここで \(K_{t}\) は生存可能領域を削る作用、\(R_{t}\) は修復・学習・冗長化・外部支援・ロールバック等によってその領域を押し広げる作用を表す。これらに対して

\[
K_{t}(A) \subseteq A, \qquad A \subseteq R_{t}(A)
\]

を仮定する。

時刻 \(t\) から \(t+1\) への更新を

\[
\begin{aligned}
V_{t}^- :&= K_{t}(V_{t}), \qquad \\
V_{t+1} :&= R_{t}(V_{t}^-)
\end{aligned}
\]

で定める。ここで \(V_{t}^-\) は収縮直後の集合、\(V_{t+1}\) は再拡大後の到達可能集合である。以後、考察する有限時間地平の範囲で、\(V_{t}\) および \(V_{t}^-\) の質量が有限かつ正であると仮定する。これにより、以下の対数比が well-defined になる。

\section{段階損失量・修復量・純損失量}\label{ux6bb5ux968eux640dux5931ux91cfux4feeux5fa9ux91cfux7d14ux640dux5931ux91cf}

各時刻 \(t\) における段階損失量を

\[
d_{t} := - \log \frac{m(V_{t}^-)}{m(V_{t})}
\]

と定める。これは収縮作用が、その段階で生存可能領域をどれだけ削ったかを測る量である。

同様に、修復作用による修復量を

\[
r_{t} := \log \frac{m(V_{t+1})}{m(V_{t}^-)}
\]

と定める。これは repair, learning, rollback, external support が、収縮後の領域をどれだけ押し広げたかを表す。ここで重要なのは、\(d_{t}\) と \(r_{t}\) の両方が質量比の対数で与えられており、同じ尺度で測られていることである。

このとき純損失量を

\[
b_{t} := d_{t} - r_{t}
\]

と定義する。累積量は

\[
\begin{aligned}
L_{n} :&= \sum_{t=0}^{n-1} d_{t}, \\
\qquad \\
R_{n}^{\mathrm{rec}} :&= \sum_{t=0}^{n-1} r_{t}, \\
\qquad \\
B_{n} :&= L_{n} - R_{n}^{\mathrm{rec}}
\end{aligned}
\]

と書く。

\section{局所対数比表現}\label{ux5c40ux6240ux5bfeux6570ux6bd4ux8868ux73fe}

収縮と再拡大を別々に定義しても、純損失量は局所的には始点と終点の対数比だけで表せる。

命題 1（純損失量の対数比表現）。 各時刻 \(t\) について

\[
\begin{aligned}
b_{t} \\
&= \\
- \log \frac{m(V_{t+1})}{m(V_{t})}
\end{aligned}
\]

が成り立つ。

証明。 定義より

\[
\begin{aligned}
b_{t} \\
&= \\
-\log \frac{m(V_{t}^-)}{m(V_{t})} \\
- \\
\log \frac{m(V_{t+1})}{m(V_{t}^-)} \\
&= \\
-\log \frac{m(V_{t+1})}{m(V_{t})}.
\end{aligned}
\]

中間集合 \(V_{t}^-\) の質量が打ち消し合うので、主張が従う。証明終。

この命題は、段階損失量と修復量を別々に導入しても、最終的に残る局所量はなお対数比であることを示す。したがって、最小理論の局所核はこの集合値力学的表現のもとでも壊れない。

\section{局所バランス恒等式}\label{ux5c40ux6240ux30d0ux30e9ux30f3ux30b9ux6052ux7b49ux5f0f}

命題 1 から、各段階の質量更新は指数型で書ける。

命題 2（局所バランス恒等式）。 各時刻 \(t\) について

\[
m(V_{t+1}) = m(V_{t}) e^{-b_{t}}
\]

が成り立つ。

証明。 命題 1 より

\[
e^{-b_{t}} = \frac{m(V_{t+1})}{m(V_{t})}
\]

であるから、両辺に \(m(V_{t})\) を掛ければよい。証明終。

この式は、収縮が支配的なら \(b_{t} > 0\)、修復が支配的なら \(b_{t} < 0\) となることを意味する。したがって、閉じた収縮過程と開いた修復過程の両方を、同じ局所更新則で扱える。

\section{符号付き指数核}\label{ux7b26ux53f7ux4ed8ux304dux6307ux6570ux6838}

局所バランス恒等式を段階ごとに積み上げると、収縮モードの指数恒等式の自然な拡張が得られる。

定理 3（符号付き指数核）。 任意の \(n \ge 0\) について

\[
m(V_{n}) = m(V_0) e^{-B_{n}}
\]

が成り立つ。

証明。 命題 2 を \(t=0,\dots,n-1\) にわたって掛け合わせると

\[
\begin{aligned}
\frac{m(V_{n})}{m(V_0)} \\
&= \prod_{0\le t<n} e^{-b_{t}} \\
&= e^{-\sum_{t=0}^{n-1} b_{t}} \\
&= e^{-B_{n}}
\end{aligned}
\]

を得る。よって

\[
m(V_{n}) = m(V_0) e^{-B_{n}}.
\]

証明終。

したがって、指数型は収縮専用の特殊な形ではない。段階損失と修復をまとめた符号付き純損失量に対しても、なお望遠鏡積として現れる。最小理論の真の核は、非負の段階損失量 \(L\) そのものというより、より一般には符号付き純損失量 \(B\) に対する指数残存則にあると読むことができる。

\section{収縮モードの回収}\label{ux53ceux7e2eux30e2ux30fcux30c9ux306eux56deux53ce}

従来の最小理論は、構造持続の集合値力学的表現の特例として直ちに回収される。

系 4（収縮モードへの還元）。 もしすべての時刻 \(t\) について

\[
R_{t} = \mathrm{id}
\]

であるなら、すべての \(t\) について \(r_{t} = 0\) であり、

\[
B_{n} = L_{n}
\]

となる。したがって

\[
m(V_{n}) = m(V_0) e^{-L_{n}}
\]

を得る。

この系が示しているのは、この集合値力学的表現が収縮理論を捨てるのではなく、その自然な拡張になっているという点である。収縮モードは、repair を持たない特例として一般理論の内部に埋め込まれる。

\section{表現共変性と弱依存安定性}\label{ux8868ux73feux5171ux5909ux6027ux3068ux5f31ux4f9dux5b58ux5b89ux5b9aux6027}

この集合値力学的表現の指数核を、Type C に近い普遍形式として強めるには、少なくとも二種類の追加条件が必要である。

第一は表現共変性である。許容された再表現 \(\phi\) に対して、ある正の係数 \(c_\phi(n) > 0\) が存在し、

\[
B_{n}(\phi \Pi) = c_\phi(n)\, B_{n}(\Pi)
\]

が成り立つとき、順序、閾値到達順、および適切に無次元化された予測量は保存される。ここで要求されるのは完全不変性ではなく、理論の関数族が壊れないという意味での共変性である。

第二は弱依存下での指数族保存である。非負の参照純損失量 \(B_{n}^{\mathrm{ref}}\) と実効純損失量 \(B_{n}^{\mathrm{eff}}\) が

\[
\begin{aligned}
|B_{n}^{\mathrm{eff}} - B_{n}^{\mathrm{ref}}| \\
\le \\
\rho B_{n}^{\mathrm{ref}}, \\
\qquad 0 \le \rho < 1
\end{aligned}
\]

を満たすなら、対応する残存量 \(P_{n} := e^{-B_{n}^{\mathrm{eff}}}\) は

\[
\begin{aligned}
e^{-(1+\rho)B_{n}^{\mathrm{ref}}} \\
\le \\
P_{n} \\
\le \\
e^{-(1-\rho)B_{n}^{\mathrm{ref}}}
\end{aligned}
\]

に挟まれる。したがって、厳密独立が成立しない場合でも、依存の効果が相対誤差として制御される限り、指数族は境界の形で安定に残る。

ここで重要なのは、これらが最小核そのものではなく、その上に重ねられる第二層の条件だということである。符号付き指数核そのものは、なお望遠鏡積の代数から従う。

\section{限界}\label{ux9650ux754c}

本補論が与えるのは、構造持続の集合値力学的表現のもとでの指数核であって、資源項まで含めた全体理論ではない。とくに

\[
S = M e^{-L}
\]

の \(M\) は、最小理論でも命題の直接の帰結ではなく、資源要因を組み込むために別途導入された拡張量であった。したがって、この集合値力学的表現の段階で Type C 的に強く言えるのは、まず

\[
m(V_{n}) = m(V_0) e^{-B_{n}}
\]

という符号付き指数核の側である。

また、本補論の枠組みも、なお観測前に固定された構造維持問題に対して読むべきである。初期構造、質量モデル、力学、時間地平を観測後に選び直してよいなら、理論は事後的適合によって空虚化する。したがって、ここでの拡張も事前固定性と表現安定性を満たす問題クラスに対するものである。

さらに、修復作用 \(R_{t}\) を導入したこと自体は、その微視的起源を説明しない。修復・学習・外部支援がなぜどの程度の修復量を生むのか、どのドメインで自然な質量モデル \(m\) が取れるのか、またどの程度まで表現共変性を示せるのかは、依然として仕様固定構造層 / 条件付き構造埋め込み層 / 構造推定層の個別課題として残る。

\section{結論}\label{ux7d50ux8ad6}

本補論では、構造持続理論の収縮モードを、収縮作用と修復作用の合成からなる構造持続の集合値力学的表現の特例として位置づけ直した。その結果、段階損失量と修復量を同じ対数尺度で定義することで、累積純損失量

\[
B_{n} = L_{n} - R_{n}^{\mathrm{rec}}
\]

に対して

\[
m(V_{n}) = m(V_0) e^{-B_{n}}
\]

という指数核がなお成立することを示した。したがって、収縮モードの指数則は、より一般の符号付き純損失量に対する指数核の特例として読むことができる。

この整理により、学習・修復・外部支援・ロールバックを理論の外側の例外として扱う必要はなくなる。今後の課題は、この集合値力学的表現の枠組みのもとで、表現共変性、弱依存安定性、そして資源項を含む開放系理論をどこまで積み上げられるかにある。

\section{本補論で証明されていること／されていないこと}\label{ux672cux88dcux8ad6ux3067ux8a3cux660eux3055ux308cux3066ux3044ux308bux3053ux3068ux3055ux308cux3066ux3044ux306aux3044ux3053ux3068}

本補論で数学的に示したのは、この集合値力学的表現の定義のもとで、純損失量の対数比表現、局所バランス恒等式、符号付き指数核、および収縮モードの回収が成り立つことである。これらは定義と仮定からの代数的帰結である。

他方、§8 で述べた表現共変性および弱依存下での指数族保存は、本補論では条件提示にとどまり、特定のドメイン族に対する定理化は今後の課題である。とくに弱依存安定性については、非負の段階損失量に関する近縁結果は既存の条件つき導出にあるが、符号付き純損失量 \(B_{n}\) に対する版は別途の検討を要する。

また、自然な質量モデルの選択、修復作用の微視的起源、資源項を含む開放系理論の公理化も、本補論の外に残る未解決課題である。したがって、構造持続理論が第二法則のような全域的普遍法則へ接続するかどうかは、これらの課題がどこまで解決されるかに依存する。

\section{第二法則級へ近づくための五つの目標定理と現在の閉鎖状況}\label{ux7b2cux4e8cux6cd5ux5247ux7d1aux3078ux8fd1ux3065ux304fux305fux3081ux306eux4e94ux3064ux306eux76eeux6a19ux5b9aux7406ux3068ux73feux5728ux306eux9589ux9396ux72b6ux6cc1}

本節では、本補論の外に残る課題を、構造持続理論を第二法則級の法則へ近づけるための目標定理群として整理する。この五つは当初、今後の研究計画を定理の形で明確化するために置かれた。現在はその一部が Lean の wrapper、reader-facing theorem map、または Phase 7 v2 の限定クラス統一 interface として閉じている。とくに、repair budget / total production / 期待値レベルの tendency / finite-horizon certificate / 登録済み限定クラスの interface closure は部分的に形式化済みである。一方、自然な質量モデルの一般同定、強い許容粗視化写像の必要十分特徴づけ、全時間一様の高確率単調性、全ドメイン普遍不等式は未解決のままである。

したがって本節の狙いは、現在得られている符号付き指数核

\[
m(V_{n}) = m(V_0) e^{-B_{n}}
\]

という balance identity を、典型的な向きを持つ law of tendency へ引き上げることにある。

目標定理 1（許容構造粒度変換の定式化）。 構造維持問題 \(\Pi\) に対して、微視状態空間 \(X\) から巨視状態空間 \(\bar X\) への写像

\[
\pi : X \to \bar X
\]

のうち、対象構造の同一性を保ち、かつ主要な順序予測を反転させないものを許容構造粒度変換と呼ぶ。許容構造粒度変換の族 \(\mathcal C\) が与えられたとき、各 \(\pi \in \mathcal C\) に対して構造粒度変換後の質量モデル \(\bar m_\pi\) と累積純損失量 \(\bar B_{n}^\pi\) が well-defined であり、それらが同一の universality class に属することを示す。

この定理の役割は、どの構造粒度変換を許すかを先に切ることである。不可逆性や不変量の議論は、許容構造粒度変換のクラスが定まってはじめて非空虚になる。

目標定理 2（repair budget / resource constraint）。 構造持続の集合値力学的表現

\[
\begin{aligned}
V_{t}^- :&= K_{t}(V_{t}), \qquad \\
V_{t+1} :&= R_{t}(V_{t}^-)
\end{aligned}
\]

において、修復作用 \(R_{t}\) は自由ではなく、外部から投入される資源入力量 \(J_{t}\) と内部可用資源 \(M_{t}\) により制約される。すなわち、ある単調関数 \(\Phi\) が存在して

\[
r_{t} \le \Phi(M_{t}, J_{t})
\]

または同値な累積形

\[
R_{n}^{\mathrm{rec}} \le \sum_{t=0}^{n-1} \Phi(M_{t}, J_{t})
\]

が成り立つことを示す。

この定理の役割は、repair を無料の操作として扱わないことである。開いた系で単調法則を得るには、修復量に予算制約または交換則が必要である。

目標定理 3（総生成量の定義と非負性候補）。 系単体の符号付き純損失量

\[
B_{n} = L_{n} - R_{n}^{\mathrm{rec}}
\]

に加えて、repair・学習・外部支援に要した資源コストの累積量 \(C_{n}\) を導入し、総生成量

\[
\Sigma_{n} := B_{n} + C_{n}
\]

を定義する。許容された資源会計のもとで、\(\Sigma_{n}\) が系と環境を合わせた構造粒度変換後の総生成量として解釈できることを示す。

この定理の役割は、単調になる候補量が生の \(B_{n}\) ではなく \(\Sigma_{n}\) かもしれないことを明示する点にある。これは閉じた系のエントロピーではなく、開いた系における総散逸量に対応する候補である。

目標定理 4（構造粒度変換後の典型的非減少）。 許容構造粒度変換 \(\pi \in \mathcal C\) と資源制約つき集合値力学的表現のもとで、構造粒度変換後の総生成量

\[
\bar \Sigma_{n}^\pi
\]

は典型的に非減少である。より具体的には、あるクラスの微視初期条件に対して

\[
\mathbb E[\bar \Sigma_{n+1}^\pi - \bar \Sigma_{n}^\pi] \ge 0
\]

または

\[
\Pr\!\bigl(\bar \Sigma_{n+1}^\pi < \bar \Sigma_{n}^\pi - \varepsilon\bigr) \le e^{-c_\varepsilon n}
\]

のような期待値版・高確率版・大偏差版のいずれかを与える。

この定理が成立するとき、balance identity ははじめて law of tendency に上がる。したがって、本節の五つのなかで最も第二法則に近い役割を担うのはこの定理である。

目標定理 5（operational theorem）。 上で定義した \(\Sigma_{n}\) またはその構造粒度変換後の版 \(\bar \Sigma_{n}\) から、観測可能な限界量を導く。最初の候補としては、次のいずれかが考えられる。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  崩壊時点境界:
\end{enumerate}

\[
\tau_{\mathrm{collapse}} \le F(\Sigma_0,\text{drift},\text{budget})
\]

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{1}
\tightlist
\item
  最小修復率定理:
\end{enumerate}

\[
\bar r_{\min} \ge R^*(\text{drift},\text{target lifetime})
\]

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{2}
\tightlist
\item
  cliff の事前識別条件: ある可観測指標 \(q_{t}\) が存在し、\(q_{t} \to q_{c}\) なら高確率で有限時間内に構造崩壊が起きる。
\end{enumerate}

この定理の役割は、抽象理論を観測可能な予測へ結びつける点にある。第二法則型の理論が信用を獲得するには、単に単調量を持つだけでなく、効率限界や崩壊境界のような operational な帰結を持つことが必要である。

以上の五つは、論理順序としては、構造粒度変換の定式化、repair 予算制約、総生成量の導入、典型的非減少、そして operational theorem の順に積み上がる。表現共変性や強い意味での不変量定理は、この系列の上で、どの量が再表現に対して保存されるかを改めて問う第二段階の課題として位置づけるのが自然である。

\section{Lean 形式化との対応}\label{lean-ux5f62ux5f0fux5316ux3068ux306eux5bfeux5fdc}

本補論の本文と Lean 形式化との対応は、現時点では次のように整理できる。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  命題 1（純損失量の対数比表現）に対応する Lean の核は、\texttt{Survival.GeneralStateDynamics.stepNetAction\_eq\_neg\_log\_feasible\_ratio} である。これは
\end{enumerate}

\[
b_{t} = -\log \frac{m(V_{t+1})}{m(V_{t})}
\]

をそのまま機械検証したものである。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{1}
\tightlist
\item
  命題 2（局所バランス恒等式）に対応する Lean の核は、\texttt{Survival.GeneralStateDynamics.feasibleMass\_succ\_eq\_mass\_mul\_exp\_neg\_stepNetAction} である。これは
\end{enumerate}

\[
m(V_{t+1}) = m(V_{t}) e^{-b_{t}}
\]

の形式化である。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{2}
\tightlist
\item
  定理 3（符号付き指数核）に対応する Lean の核は、\texttt{Survival.GeneralStateDynamics.feasibleMass\_eq\_initial\_mul\_exp\_neg\_cumulativeNetAction} である。これは
\end{enumerate}

\[
m(V_{n}) = m(V_0) e^{-B_{n}}
\]

を望遠鏡積として機械検証したものである。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{3}
\item
  系 4（収縮モードへの還元）に対応する Lean の核は、\texttt{Survival.GeneralStateDynamics.feasibleMass\_eq\_initial\_mul\_exp\_neg\_cumulativeLoss\_of\_pureContraction} である。これは \(R_{t} = \mathrm{id}\) の特例で旧来の収縮モードへ戻ることを表す。
\item
  §12 の目標定理 2（repair budget / resource constraint）に対する Lean の現在の足場は、\texttt{Survival.ResourceBudget}, \texttt{Survival.ResourceBoundedDynamics.stepTotalProduction\_nonneg}, \texttt{Survival.ResourceBoundedStochasticCollapse.ResourceBoundedStepModelAzuma.of\_boundedIncrements} である。ここでは repair が無料ではないという制約を、総生成量の一歩増分非負性へつなぐ層が形式化されている。
\item
  §12 の目標定理 3（総生成量の定義と非負性候補）に対する Lean の現在の足場は、\texttt{Survival.TotalProduction.stepTotalProduction}, \texttt{Survival.TotalProduction.cumulativeTotalProduction}, \texttt{Survival.TotalProduction.cumulativeTotalProduction\_eq\_cumulativeLoss\_add\_cumulativeRepairSlack} である。これにより \(\Sigma_{n} = B_{n} + C_{n}\) の定義層と、その分解が機械化されている。
\item
  §12 の目標定理 4（構造粒度変換後の典型的非減少）に対する Lean の現在の足場は、\texttt{Survival.CoarseTypicalNondecrease}, \texttt{Survival.TypicalNondecrease.expectedCumulative\_monotone}, \texttt{Survival.ResourceBoundedDynamics.cumulativeTotalProduction\_monotone}, \texttt{Survival.ProbabilityConnection.expectedTotalProduction\_monotone\_of\_ae\_nonnegative\_increment}, \texttt{Survival.ResourceBoundedStochasticCollapse.expectedCumulative\_monotone} である。2026-04-22 の M1 gap analysis により、目標定理 4 のうち期待値レベルの tendency schema は、 これらの既存 Lean 定理を通じて formally accessible であることを確認した。すなわち、 一歩ごとの total production の非負性、またはそれを含意する resource-bounded assumption のもとで、 expected cumulative total production が単調になるという主張は新規 Lean 証明を必要としない。 残る作業は、論文側の文言を stepwise / adjacent-prefix の非負一歩増分に合わせること、 reader-facing な対応表を維持すること、および高確率 stopped-collapse schema を期待値版 tendency schema と混同しないことである。
\end{enumerate}

具体的な対応は \texttt{lean/UNIVERSALITY\_GAP\_MAP.md} および \texttt{lean/PAPER\_MAPPING.md} にまとめた。そこでは、期待値版 tendency、高確率 stopped-collapse、Bernoulli-CSP の bad-event drift / Chernoff collapse tendency を別 schema として扱う。特に Bernoulli-CSP 側の collapse/drift tendency は、repair-dominance 語彙へ無理に押し込まず、有限地平線の bad-event exposure に固有の形式として読む。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{7}
\tightlist
\item
  §12 の目標定理 5（operational theorem）に対する Lean の現在の足場は、
\end{enumerate}

\[
\begin{aligned}
\texttt{Survival.MinimumRepairRate}, \\
\qquad \\
\texttt{Survival.CollapseTimeBound}, \\
\qquad \\
\texttt{Survival.CliffWarning}, \\
\qquad \\
\texttt{Survival.ResourceBoundedStochasticCollapse.stoppedCollapseWithFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{Survival.ResourceBoundedStochasticCollapse.hittingTimeBeforeHorizonWithFailureBound\_of\_expectedMargin}
\end{aligned}
\]

およびそれらの deterministic / coarse / stochastic wrapper 群である。したがって、operational 側については、最小修復率、崩壊時点境界、cliff 事前識別条件の三系統がすでに Lean 上で見取り図を持っている。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{8}
\tightlist
\item
  さらに、条件付き期待値による submartingale drift を total production 側へ自動接続する層として、
\end{enumerate}

\[
\texttt{Survival.ResourceBoundedConditionalAzuma}
\]

がある。ここでは

\[
\texttt{StepModelConditionalAzumaData.toResourceBoundedStepModelAzuma}
\]

により、bounded increments、resource-boundedness、conditional drift、lower-tail witness から

\[
\texttt{ResourceBoundedStepModelAzuma}
\]

が自動生成され、そこから初期マージン版の stopped collapse および hitting-time 境界が直接導かれる。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{9}
\tightlist
\item
  さらに、
\end{enumerate}

\[
\texttt{Survival.ConstantDriftExample}
\]

は、actual probability space 上の constant-drift process を用いて、 conditional Azuma 層から high-probability stopped collapse / hitting-time bound までを end-to-end に instantiate する最小 concrete example である。したがって、 ここで構成された抽象 interface 群は、定義層にとどまらず、少なくとも一つの具体モデルでは 機械検証された形で最後まで接続している。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{10}
\tightlist
\item
  さらに、
\end{enumerate}

\[
\texttt{Survival.ToyRandomWalk}
\]

は、nonnegative integrable increments によって駆動される stochastic total production を

\[
\Sigma_{n}(\omega)=s_0+\sum_{t<n}\xi_{t}(\omega)
\]

の形で構成し、そこから期待累積総生成量の単調性、および構造粒度変換 compatibility の下での 期待累積量の単調性を直接導く最小 stochastic example である。 したがって、constant-drift の deterministic concrete example に加えて、非自明な stochastic total production 側にも、構造粒度変換後の典型的非減少の適用例が一つ追加されたことになる。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{11}
\tightlist
\item
  さらに、
\end{enumerate}

\[
\texttt{Survival.MarkovRepairFailureExample}
\]

は、有限状態

\[
\{\text{failure}, \text{idle}, \text{repair}\}
\]

の path process と state-dependent emission を用いて、repair / failure の語彙を持つ Markov-style な stochastic total production の具体例を与える。ここでは actual Markov measure そのものの構成までは行わないが、transition law を bookkeeping data として保持したうえで、 finite-state な emitted total production から expected cumulative monotonicity と coarse expected cumulative monotonicity を直接導く層が形式化されている。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{12}
\tightlist
\item
  さらに、
\end{enumerate}

\[
\texttt{Survival.FiniteStateMarkovRepairChain}
\]

は、初期分布 \texttt{init\ :\ PMF\ RepairState} と状態依存遷移 \texttt{step\ :\ \$RepairState\ -\textgreater{}\ PMF\$\ RepairState} から finite horizon の trajectory PMF を再帰的に構成し、その \texttt{PMF.toMeasure} を actual probability measure として用いることで、有限状態 repair / failure 例を 「Markov-style な bookkeeping data」から「実際の path measure を持つ concrete stochastic process」 へ一段進めている。ここでは infinite-horizon の Ionescu--Tulcea 構成までは導入しないが、 少なくとも有限時間崩壊・有限 horizon の total production 議論に必要な範囲では、 初期分布と遷移 PMF から path-space measure が実際に構成され、その上で expected cumulative monotonicity と coarse expected cumulative monotonicity が形式化されている。

\section{さらに、}\label{ux3055ux3089ux306b}

\[
\texttt{Survival.FiniteStateMarkovCollapse}
\]

は、この finite-horizon actual Markov chain に対して

\[
\texttt{StepModelLowerTailWitness}
\]

を与えると、

\[
\begin{aligned}
\texttt{markov\_stoppedCollapseWithFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{markov\_stoppedCollapseWithFailureBound\_of\_initialExpectedMargin},
\end{aligned}
\]

\[
\begin{aligned}
\texttt{markov\_hittingTimeBeforeHorizonWithFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{markov\_hittingTimeBeforeHorizonWithFailureBound\_of\_initialExpectedMargin}
\end{aligned}
\]

を通じて、actual finite-state Markov path measure から stopped collapse と hitting-time bound を直接引ける層を与える。ここでは

\[
\texttt{incrementBound(E)=netActionBound(E)+costBound(E)}
\]

という有限状態 emission 由来の一様 bounded-increment witness と、 statewise nonnegative total production から来る resource-boundedness が自動で供給され、 残る probabilistic 入力が lower-tail witness のみに縮約されている。

\section{さらに、}\label{ux3055ux3089ux306b-1}

\[
\texttt{Survival.FiniteStateMarkovFlatWitness}
\]

は、各状態で

\[
\mathrm{netActionOf}(s)+\mathrm{costOf}(s)=\sigma
\]

が成り立つ flat total emission のクラスを切り出し、その場合には cumulative total production が pathwise に deterministic になることを示す。そこから

\[
\begin{aligned}
\texttt{lowerTailWitness}, \\
\qquad \\
\texttt{resourceBoundedStepModelAzuma}
\end{aligned}
\]

が自動生成され、finite-state actual Markov chain から lower-tail witness を追加の確率論抜きに 組める最初の concrete subclass が得られる。

\section{さらに、}\label{ux3055ux3089ux306b-2}

\[
\texttt{Survival.FiniteStateMarkovConditionalAzuma}
\]

は、finite-horizon actual Markov path measure の上で cumulative total production に対する filtration, adaptedness, conditional submartingale drift を与えると、

\[
\texttt{MarkovConditionalAzumaData.toStepModelConditionalAzumaData}
\]

を通じて generic な

\[
\texttt{StepModelConditionalAzumaData}
\]

へ自動変換し、そこから

\[
\texttt{expectedCumulative\_monotone\_of\_conditionalAzuma},
\]

\[
\begin{aligned}
\texttt{markov\_stoppedCollapseWithFailureBound\_of\_initialExpectedMargin}, \\
\qquad \\
\texttt{markov\_hittingTimeBeforeHorizonWithFailureBound\_of\_initialExpectedMargin}
\end{aligned}
\]

を actual finite-state Markov chain 上で直接引ける層を与える。これにより、 conditional drift による expected monotonicity と high-probability collapse / hitting-time bound の 橋が、finite-state path measure 上でも抽象 wrapper を介さず読める形になった。

さらに今回の拡張では、

\[
\begin{aligned}
\texttt{lowerTailWitnessOfAzumaHoeffding}, \\
\qquad \\
\texttt{MarkovConditionalAzumaConcentrationData}
\end{aligned}
\]

が導入され、finite-state actual Markov chain 上の具体的な

\[
\texttt{AzumaHoeffdingConcentration}
\]

から lower-tail witness を自動生成し、そのまま conditional Azuma 層へ流せるようになった。 したがって、この段階では Markov path-space 上の conditional drift と concentration object が与えられれば、 中間の witness を手で書かずに initial-margin collapse / hitting-time bound まで接続できる。

さらに一般化として、

\[
\begin{aligned}
\texttt{MarkovConditionalAzumaData.toAzumaHoeffdingConcentration}, \\
\qquad \\
\texttt{MarkovConditionalAzumaData.toMarkovConditionalAzumaConcentrationData}
\end{aligned}
\]

が追加され、finite-state actual Markov chain 上で lower-tail witness が与えられていれば、それ自体から generic な concentration object を自動構成し、concentration 経由の wrapper へ持ち上げられるようになった。

さらに、

\[
\texttt{Survival.FiniteStateMarkovDeterministicWitness}
\]

では、cumulative total production が pathwise に deterministic であること自体を criterion として切り出し、

\[
\begin{aligned}
\texttt{lowerTailWitness}, \\
\qquad \\
\texttt{toMarkovConditionalAzumaData}, \\
\qquad \\
\texttt{toMarkovConditionalAzumaConcentrationData}
\end{aligned}
\]

を自動生成できるようにした。これにより、flat total emission は「deterministic cumulative」の具体例として再解釈され、 lower-tail witness の自動生成条件が一段一般化された。

\section{さらに、}\label{ux3055ux3089ux306b-3}

\[
\texttt{Survival.ThreeStateTransitionExample}
\]

は、3 状態

\[
\text{failure}\to\text{idle}\to\text{repair}\to\text{failure}
\]

の deterministic cycle を持つ具体的な遷移行列と、flat total emission を組み合わせた actual finite-state Markov chain の end-to-end 例である。ここでは

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_initialExpectedMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_initialExpectedMargin}
\end{aligned}
\]

が実際に具体 chain 上で導かれており、finite-state path measure から collapse / hitting-time bound までが concrete に一本で接続している。

今回の拡張ではこれに加えて、

\[
\begin{aligned}
\texttt{concentration}, \\
\qquad \\
\texttt{conditionalAzumaConcentrationData},
\end{aligned}
\]

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_initialExpectedMargin\_concentration}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_initialExpectedMargin\_concentration}
\end{aligned}
\]

が追加され、3 状態 concrete chain から actual Markov path measure 上の \texttt{AzumaHoeffdingConcentration} を直接構成し、それを conditional Azuma 層へ流した上で collapse / hitting-time bound を end-to-end に導く経路も明示化された。

さらに現在の Lean 側では、この 3 状態例は

\[
\texttt{FiniteStateMarkovDeterministicWitness}
\]

の deterministic-cumulative criterion を通じて組み直されており、flat total emission がより一般の自動 witness 生成条件の具体例であることがコード上でも明示されている。

\section{さらに、}\label{ux3055ux3089ux306b-4}

\[
\texttt{Survival.FiniteStateMarkovStationaryProduction}
\]

は、finite-state actual Markov chain に対して、まず各時刻の state marginal

\[
\mu_{t} = \texttt{stateMarginal M t}
\]

と状態平均

\[
\texttt{stateAverage}
\]

を用いて

\[
\begin{aligned}
\texttt{meanExpectedIncrement}, \\
\qquad \\
\texttt{meanExpectedProcess}
\end{aligned}
\]

を構成し、

\[
E[\Sigma_{n}] = s_0 + \sum_{t<n}\langle \mu_{t},\phi\rangle
\]

に対応する一般 mean-production formula を期待値レベルで形式化する。そのうえで定常分布

\[
\texttt{StationaryData}
\]

を与えたとき、状態平均

\[
\begin{aligned}
\texttt{stateAverage}, \\
\qquad \\
\texttt{stationaryMean}
\end{aligned}
\]

を用いて one-step total production の定常平均流を定義し、

\[
\texttt{stationaryExpectedProcess}
\]

として有限 horizon 上の線形 expected center を構成する。ここでは

\[
\texttt{meanExpectedCumulative\_eq\_initial\_add\_active\_sum\_of\_le}
\]

により finite-prefix family の上での一般期待値公式が与えられ、

\[
\begin{aligned}
\texttt{stateMarginal\_eq\_stationary}, \\
\qquad \\
\texttt{stateMarginal\_totalProduction\_eq\_stationaryMean}
\end{aligned}
\]

により「stationary start では各時刻の状態 marginal で見た平均 total production が定常平均に一致する」ことが形式化される。さらに

\[
\begin{aligned}
\texttt{meanExpectedCumulative\_eq\_stationaryExpectedCumulative}, \\
\qquad \\
\texttt{meanExpectedCumulative\_eq\_initial\_add\_linear\_of\_stationary}
\end{aligned}
\]

によって、一般 mean-production process から stationary corollary

\[
E[\Sigma_{n}] = s_0 + n\langle \pi,\phi\rangle
\]

が導かれ、

\[
\texttt{stationaryExpectedCumulative\_monotone}
\]

により、その定常平均が非負である限り expected cumulative total production が 有限 horizon の active window 上で単調に増加することが示される。したがって、 finite-state microfoundation の最初の形として、「崩壊境界」だけでなく「一般期待値公式」と 「平均的な向き」を与える定常生産則の層が Lean 上に追加されたことになる。

18.5. これに加えて、

\[
\texttt{Survival.FiniteStateMarkovMeanBridge}
\]

では、finite-horizon の actual Markov path measure と、上で定義した一般 mean-production / stationary-production center との橋渡しが形式化された。ここでは

\[
\texttt{stateProjectionPMF\_eq\_stateMarginal}
\]

により、path-space \texttt{\textbackslash{}texttt\{PMF\}} 上で時刻 \texttt{t} の状態を射影した分布が

\[
\texttt{stateMarginal M t}
\]

に一致することが示される。その結果として

\[
\begin{aligned}
\texttt{expectedIncrement\_eq\_meanExpectedIncrement}, \\
\qquad \\
\texttt{expectedCumulative\_eq\_meanExpectedCumulative}
\end{aligned}
\]

により、actual path-space 上の total production の期待増分・期待累積量が、抽象的に定義された \texttt{\textbackslash{}texttt\{meanExpectedProcess\}} と正確に一致することが Lean 上で接続された。さらに

\[
\begin{aligned}
\texttt{expectedCumulative\_eq\_stationaryExpectedCumulative}, \\
\qquad \\
\texttt{expectedCumulative\_eq\_initial\_add\_linear\_of\_stationary}
\end{aligned}
\]

によって、stationary start の場合には actual path-space の期待累積 total production が \texttt{\textbackslash{}texttt\{stationaryExpectedProcess\}}、ひいては

\[
E[\Sigma_{n}] = s_0 + n\langle \pi,\phi\rangle
\]

の形の linear center に一致することが concrete path measure の側からも回収される。したがって、 \texttt{FiniteStateMarkovStationaryProduction} が与える mean / stationary center は、単なる abstract expectation interface ではなく、actual finite-horizon Markov chain の path-space expectation に よって直接実現されることが Lean 上で保証されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-5}

\[
\texttt{Survival.FiniteStateMarkovErgodicProduction}
\]

では、full ergodic theorem へ進む前の最小の expectation-level 長時間法則が追加された。ここでは finite-horizon framework を保ったまま prefix family

\[
\texttt{PrefixFamily}
\]

を導入し、各 prefix が必ず active window に含まれるようにした上で、

\[
\texttt{stationaryPrefixAverage}
\]

を

\[
\frac{E[\Sigma_{n+1}]}{n+1}
\]

に対応する量として定義している。そのうえで

\[
\texttt{stationaryPrefixAverage\_eq\_mean\_add\_correction}
\]

により

\[
\begin{aligned}
\frac{E[\Sigma_{n+1}]}{n+1} \\
&= \\
\langle \pi,\phi\rangle + \frac{s_0}{n+1}
\end{aligned}
\]

に対応する exact decomposition が与えられ、

\[
\texttt{tendsto\_stationaryPrefixAverage}
\]

により、その正規化 expected cumulative total production が stationary mean production

\[
\texttt{stationaryMean}
\]

へ収束することが Lean 上で形式化された。したがって、finite-state Markov の concrete path-space, collapse / hitting-time 境界, general mean-production formula の上に、expectation-level の Cesàro 型 long-time law が一段積み上がったことになる。

\section{さらに、}\label{ux3055ux3089ux306b-6}

\[
\texttt{Survival.FiniteStateMarkovPositiveDriftCollapse}
\]

では、flat total emission

\[
\texttt{netActionOf}(s) + \texttt{costOf}(s) = \sigma
\]

の有限状態 Markov 連鎖に対して、positive drift から high-probability collapse / hitting-time bound を直接引く wrapper 層が追加された。ここでは

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_activeLinearMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_activeLinearMargin}
\end{aligned}
\]

により、actual path-space 上で

\[
E[\Sigma_{n}] = s_0 + \texttt{activeSteps}(N,n)\,\sigma
\]

という exact linear center をそのまま Azuma / stopping-time API に差し込める。 さらに active window の内部では

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_linearMargin\_of\_le}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_linearMargin\_of\_le}
\end{aligned}
\]

によって

\[
E[\Sigma_{n}] = s_0 + n\sigma
\]

の形で読める境界に整理される。また

\[
\texttt{stationaryMean\_eq\_sigma\_of\_flatEmission}
\]

により、flat emission では stationary mean production がちょうど \texttt{\textbackslash{}sigma} に一致することが示され、

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_stationaryMean\_of\_le}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_stationaryMean\_of\_le}
\end{aligned}
\]

として、stationary mean の言葉で same high-probability bound を読む corollary も追加された。 したがって、finite-state Markov の concrete path-space, mean-production, Cesàro 型長時間法則の上に、 positive drift law から finite-horizon collapse / hitting-time 境界を引く層が一段積み上がったことになる。

\section{さらに、}\label{ux3055ux3089ux306b-7}

\[
\texttt{Survival.FiniteStateMarkovStationaryMeanCollapse}
\]

では、上の flat emission 専用の drift law を、一般の state-dependent emission に対して stationary start のもとで読み直す wrapper 層が追加された。ここでは新しい probabilistic input を増やさず、

\[
\texttt{FiniteStateMarkovMeanBridge}
\]

が与える

\[
E[\Sigma_{n}] = s_0 + \texttt{activeSteps}(N,n)\,\texttt{stationaryMean}
\]

という exact expected center と、

\[
\texttt{FiniteStateMarkovCollapse}
\]

が与える actual finite-state Markov chain 上の high-probability collapse / hitting-time API を 直接接続している。具体的には

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_activeStationaryMean}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_activeStationaryMean}
\end{aligned}
\]

により、actual path-space 上の期待累積 total production が stationary mean の linear center を持つとき、 そのまま stopped-collapse / hitting-time 境界へ流せるようになった。さらに active window の内部では

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_stationaryMean\_of\_le}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_stationaryMean\_of\_le}
\end{aligned}
\]

によって

\[
E[\Sigma_{n}] = s_0 + n\langle \pi,\phi\rangle
\]

の形で読める exact linear center から同じ境界を得ることができる。したがって、 state-dependent emission を持つ finite-state Markov chain についても、「stationary mean が 長時間の平均的な向きを与え、その exact expected center が finite-horizon collapse / hitting-time bound に接続される」という第二法則候補の中核部分が、Lean 上で一段一般化されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-8}

\[
\texttt{Survival.FiniteStateMarkovStationaryLongTimeConcentration}
\]

では、上の stationary-mean collapse wrapper を finite-prefix family の言葉で長時間読みに 組み替える層が追加された。ここでは

\[
\texttt{prefixLinearCenter\_eq\_mul\_stationaryPrefixAverage}
\]

により、stationary start の exact linear center

\[
s_0 + (n+1)\langle \pi,\phi\rangle
\]

が

\[
(n+1)\,\texttt{stationaryPrefixAverage}
\]

の形に正確に書き換えられる。そのうえで

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_stationaryPrefixAverage}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_stationaryPrefixMean}
\end{aligned}
\]

により、finite-horizon actual Markov chain 上の stopped-collapse / hitting-time 境界が、 prefix-normalized stationary average を通じて長時間版の concentration の形に再読できるようになった。 したがって、finite-horizon collapse API と expectation-level long-time law のあいだに、 stationary mean positive drift を prefix family の上で接続する橋が Lean 上で与えられたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-9}

\[
\texttt{Survival.ThreeStateStateDependentExample}
\]

では、flat total emission に依らない genuinely state-dependent emission を持つ concrete な 3状態 finite-state Markov chain の end-to-end 例が追加された。ここでは deterministic cycle \texttt{failure\ -\textgreater{}\ \$idle\ -\textgreater{}\ repair\$\ -\textgreater{}\ failure} を持つ actual finite-horizon path measure を構成し、その path-space measure が

\[
\texttt{canonicalPath}
\]

上の Dirac mass になることを

\[
\texttt{pathPMF\_eq\_pure\_canonicalPath}
\]

として形式化している。その結果、

\[
\texttt{activeCenter}
\]

で与えた concrete な state-dependent cumulative total production の中心軌道に対して

\[
\texttt{expectedCumulative\_eq\_activeCenter}
\]

が成り立ち、singleton support event から

\[
\texttt{lowerTailWitness}
\]

が自動で構成される。さらに

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_activeCenter}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_activeCenter}
\end{aligned}
\]

により、actual finite-horizon Markov chain + state-dependent emission + concrete center から high-probability collapse / hitting-time bound までが end-to-end で Lean 上に接続された。 したがって、抽象 API だけでなく、state-dependent emission を持つ concrete finite-state chain に おいても第二法則候補の collapse 側の読みが実際に作動することが、具体例として確認されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-10}

\[
\texttt{Survival.ResourceBudgetToTotalProductionDrift}
\]

では、resource budget と total production のあいだの最小の連結定理が独立化された。ここでは

\[
\texttt{stepTotalProduction\_lowerBound\_of\_stepLoss\_lowerBound}
\]

により、repair slack の非負性

\[
\texttt{stepRepairSlack} = \texttt{stepCost} - \texttt{stepGain} \ge 0
\]

のもとで、任意の one-step contraction loss 下界

\[
\alpha \le \texttt{stepLoss}
\]

がそのまま

\[
\alpha \le \texttt{stepTotalProduction}
\]

へ持ち上がることが示される。さらに

\[
\texttt{cumulativeTotalProduction\_lowerBound\_of\_stepLoss\_lowerBound}
\]

により、この pointwise な loss 下界は cumulative total production の線形下界へも持ち上がる。したがって、 「resource budget は drift を直接は作らないが、domain-specific な contraction 下界を \(\Sigma\) 側の drift 下界へ接続する」という bridge layer が Lean 上で明示化されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-11}

\[
\texttt{Survival.SATDriftLowerBound}
\]

では、random 3-SAT の第一モーメント法から現れる per-clause information loss

\[
\log(8/7)
\]

が、explicit な drift parameter として切り出された。ここでは

\[
\texttt{random3ClauseDrift}
\]

を

\[
\texttt{infoLoss}(7/8)
\]

として定義し、

\[
\begin{aligned}
\texttt{random3ClauseDrift\_eq\_log}, \\
\qquad \\
\texttt{random3ClauseDrift\_pos}
\end{aligned}
\]

により、それが正確に \(\log(8/7)\) であり、しかも厳密に正であることが示される。さらに

\[
\texttt{cumulativeInfoLoss\_random3Clause}
\]

により、\(m\) 個の一様 random 3-clause に対する cumulative information loss が

\[
m \log(8/7)
\]

に正確に一致することが、\texttt{SATFirstMoment} の一般定理から corollary として回収される。したがって、 SAT においては drift parameter が外から与えられるのではなく、第一モーメント法そのものから内部的に 導出されることが Lean 上で独立化されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-12}

\[
\texttt{Survival.SATPositiveDriftCollapse}
\]

では、上で抽出した SAT drift

\[
\texttt{random3ClauseDrift} = \log(8/7)
\]

を \texttt{ConstantDriftExample} の collapse / hitting-time API に直接差し込む thin wrapper が追加された。 ここでは

\[
\texttt{random3ClauseStepModel}
\]

を random 3-SAT first-moment drift を持つ constant-drift model として定め、

\[
\texttt{expectedCumulative\_eq}
\]

により、その expected cumulative total production が

\[
s_0 + n\log(8/7)
\]

の exact linear center を持つことが示される。そのうえで

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_initialExpectedMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_initialExpectedMargin}
\end{aligned}
\]

により、SAT 由来の explicit drift を持つ process から high-probability collapse / hitting-time bound が 直接回収される。したがって、SAT については「第一モーメント法から drift を内部導出し、その drift を collapse theory に流す」という domain-specific な一連の流れが、Lean 上でひとまず end-to-end に 接続されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-13}

\[
\texttt{Survival.ResourceBudgetToSigmaDrift}
\]

では、§12 で目標として掲げた

\[
\begin{aligned}
\text{ResourceBudget} + \text{Typical contraction lower bound} \\
\Longrightarrow \\
\mathbb E[\Delta \Sigma_{t}] \ge \alpha
\end{aligned}
\]

という expectation-level の連結定理が、deterministic embedding の形で独立化された。ここでは

\[
\texttt{TypicalContractionLowerBound}
\]

を

\[
\forall t,\ \alpha \le \texttt{stepLoss}
\]

として定義し、

\[
\texttt{expectedIncrement\_lowerBound\_of\_stepLoss\_lowerBound}
\]

により、resource budget のもとでこの contraction 下界がそのまま

\[
\alpha \le \mathbb E[\Delta \Sigma_{t}]
\]

へ持ち上がることが示される。さらに

\[
\begin{aligned}
\texttt{expectedDriftLowerBound\_of\_typicalContraction}, \\
\qquad \\
\texttt{expectedCumulative\_lowerBound\_of\_typicalContraction}
\end{aligned}
\]

により、one-step の lower bound は cumulative expected total production の線形下界へ持ち上がる。 そのうえで

\[
\begin{aligned}
\texttt{submartingaleLike\_of\_nonneg\_typicalContraction}, \\
\qquad \\
\texttt{expectedCumulative\_monotone\_of\_nonneg\_typicalContraction}
\end{aligned}
\]

により、\(\alpha \ge 0\) なら expected cumulative total production が単調になることも示される。 したがって、resource budget は drift を直接作るのではなく、「domain-specific な contraction 下界を \(\Sigma\) 側の expected drift 下界へ接続する bridge」として機能することが、Lean 上でも expectation / submartingale の言葉で明示化されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-14}

\[
\texttt{Survival.SATUnconditionalTendency}
\]

では、SAT の第一モーメント法から導出された

\[
\texttt{random3ClauseDrift}=\log(8/7)
\]

を、外部から与えた drift ではなく、domain-specific に内部導出された drift として使う 期待値レベルの傾向が独立化された。ここでは

\[
\texttt{expectedIncrement\_eq\_random3ClauseDrift}
\]

により、SAT specialization の one-step expected total-production drift が各時刻で正確に \(\log(8/7)\) に一致することが示される。その結果、

\[
\begin{aligned}
\texttt{submartingaleLike\_random3ClauseStepModel}, \\
\qquad \\
\texttt{expectedCumulative\_monotone\_random3ClauseStepModel}
\end{aligned}
\]

により、SAT first-moment process は追加の drift 仮定なしに expectation-level で単調となる。 さらに

\[
\texttt{expectedCumulative\_lower\_linear\_random3ClauseStepModel}
\]

により、expected cumulative total production は

\[
s_0 + n\log(8/7)
\]

という exact linear center から直接下界づけられる。したがって、SAT においては 「第一モーメント法から drift 符号を内部導出し、それを expectation-level law of tendency として読む」 という最初の無条件傾向法則が、Lean 上で独立の層として表現されたことになる。

\section{さらに、}\label{ux3055ux3089ux306b-15}

\[
\texttt{Survival.SATClauseExposureProcess}
\]

では、SAT の drift を単なる constant-drift wrapper としてではなく、actual な clause-exposure path space の上で直接読む層が追加された。ここでは、各 exposed clause の outcome を

\[
\begin{aligned}
\texttt{sat} \quad (\text{prob. } 7/8), \\
\qquad \\
\texttt{unsat} \quad (\text{prob. } 1/8)
\end{aligned}
\]

とする有限長 trajectory の PMF

\[
\texttt{pathPMF},\qquad \texttt{pathMeasure}
\]

が定義され、その actual probability space の上に SAT clause-exposure process

\[
\texttt{stepModel}
\]

が構成される。per-clause total production は realized outcome に依らず

\[
\texttt{random3ClauseDrift}=\log(8/7)
\]

であるため、cumulative total production は pathwise に deterministic となり、

\[
\begin{aligned}
\texttt{expectedCumulative\_eq}, \\
\qquad \\
\texttt{expectedIncrement\_eq\_random3ClauseDrift}
\end{aligned}
\]

により、actual clause-exposure path space 上でも exact linear center と positive drift が回収される。 さらに

\[
\texttt{lowerTailWitness}
\]

が automatic に構成され、

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{stoppedCollapseWithFailureBound\_of\_initialExpectedMargin},
\end{aligned}
\]

\[
\begin{aligned}
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_initialExpectedMargin}
\end{aligned}
\]

により、\texttt{ConstantDriftExample} を経由せず、actual SAT clause-exposure path space から直接 collapse / hitting-time bound までが接続された。したがって、SAT については 「explicit drift を wrapper に差し込む」段階から、 「actual clause-exposure probability space を持つ direct SAT wrapper」へ一段進んだことになる。

\section{さらに、}\label{ux3055ux3089ux306b-16}

\[
\texttt{Survival.SATStateDependentClauseExposure}
\]

では、同じ actual SAT clause-exposure path space の上に、realized outcome 依存の non-flat additive functional が導入された。ここでは、clause outcome ごとの total production を \texttt{Emission.totalOf\ :\ ClauseOutcome\ -\textgreater{}\ Real} として与え、

\[
\texttt{stepModel}
\]

により actual path measure 上の stochastic total production process を構成する。 このとき

\[
\texttt{outcomeProjectionPMF\_eq\_clausePMF}
\]

により、active prefix 上の各時刻 marginal は依然として one-step law \(\texttt{clausePMF}\) に一致し、

\[
\texttt{expectedIncrement\_eq\_emissionMean\_of\_le}
\]

により、expected one-step total production はその clause-level mean

\[
\texttt{emissionMean}
\]

に一致する。したがって、actual path space を固定したまま、observable だけを state-dependent に変えた non-flat additive functional でも、expected drift は finite-state marginal average として読める。

さらに concrete な SAT specialization として

\[
\texttt{oneSidedUnsatEmission}
\]

が追加され、これは

\[
\begin{aligned}
\texttt{sat} \mapsto 0, \\
\qquad \\
\texttt{unsat} \mapsto 8\log(8/7)
\end{aligned}
\]

という genuinely non-flat な emission を与える。このとき

\[
\texttt{emissionMean\_eq\_random3ClauseDrift}
\]

により、その clause-level mean はなお

\[
\log(8/7)
\]

に一致する。したがって

\[
\texttt{expectedIncrement\_eq\_random3ClauseDrift\_of\_le}
\]

により、active prefix 上では non-flat process でも expected one-step drift は従来と同じ \(\log(8/7)\) となる。また

\[
\texttt{expectedCumulative\_eq\_initial\_add\_linear\_of\_le}
\]

により、active prefix では expected cumulative total production の exact linear center

\[
s_0 + n \log(8/7)
\]

も回収される。他方、実現値レベルでは process 自体が realized outcome に依存するため、ここで初めて SAT は flat first-moment wrapper を超えて、「actual clause-exposure path space 上の non-flat stochastic additive functional」を持つ段階へ進んだことになる。

\section{さらに、}\label{ux3055ux3089ux306b-17}

\[
\texttt{Survival.SATStateDependentUnconditionalTendency}
\]

では、この non-flat actual SAT clause-exposure process そのものについて、expectation-level の law-of-tendency が独立に切り出された。具体的には、

\[
\begin{aligned}
\texttt{submartingaleLike\_stepModel}, \\
\qquad \\
\texttt{expectedCumulative\_monotone\_stepModel}
\end{aligned}
\]

により、\texttt{sat\ \textbackslash{}mapsto\ 0}, \texttt{unsat\ \textbackslash{}mapsto\ 8\textbackslash{}log(8/7)} という genuinely state-dependent な observable を持つ actual process が、それでもなお外部 drift パラメータを必要とせず submartingale-like になることが示される。また

\[
\texttt{expectedIncrement\_pos\_of\_le}
\]

により active prefix 上での strict positivity が回収され、

\[
\texttt{expectedCumulative\_eq\_initial\_add\_linear}
\]

により exact linear center

\[
s_0 + n\log(8/7)
\]

もそのまま保たれる。したがって SAT については、constant-drift wrapper ではなく、 actual clause-exposure path space 上の non-flat stochastic additive functional に対しても、 expectation-level の unconditional tendency law が成立している。

\section{さらに、}\label{ux3055ux3089ux306b-18}

\[
\texttt{Survival.SATStateDependentAzuma}
\]

では、この non-flat actual SAT clause-exposure process を、そのまま generic な Azuma / collapse API に接続する SAT-specific bridge 層が与えられた。ここでは

\[
\texttt{incrementBound}
\]

により concrete emission \(\texttt{oneSidedUnsatEmission}\) から bounded increment が自動生成され、

\[
\texttt{stepModelAzumaData}
\]

により actual SAT step model が generic \texttt{StepModelAzumaData} に packaged される。その上で

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_expectedMargin}
\end{aligned}
\]

により、SAT-specific lower-tail witness が与えられれば、actual non-flat SAT process に対して stopped collapse / hitting-time bound を generic API へ直接流せる。また

\[
\begin{aligned}
\texttt{stoppedCollapseWithFailureBound\_of\_activeLinearMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_activeLinearMargin}
\end{aligned}
\]

により、active prefix 上で既に得られている exact linear center

\[
s_0 + n\log(8/7)
\]

をそのまま高確率境界の入力へ渡す wrapper も与えられる。したがって SAT 側では、actual clause-exposure path space と non-flat additive functional を保ったまま、generic concentration / collapse stack への接続が完了している。なお、この時点では lower-tail witness 自体はまだ外部入力であり、SAT 固有の nontrivial concentration の導出は次段階の課題として分離されている。

\section{さらに、}\label{ux3055ux3089ux306b-19}

\[
\texttt{Survival.SATStateDependentExactConcentration}
\]

では、この same non-flat actual SAT clause-exposure process に対して、Azuma 型の閉形式上界をまだ 課さない \textbf{exact lower-tail concentration layer} が追加された。ここでは

\[
\texttt{exactLowerTailEvent}
\]

が

\[
\mathbb E[\Sigma_{n}]-r \le \Sigma_{n}(\omega)
\]

という actual lower-tail threshold event をそのまま good event として定義し、

\[
\texttt{exactFailureBound}
\]

がその補集合の actual path-measure probability を failure profile として記録する。その上で

\[
\texttt{exactExpectationLowerConcentration}
\]

により、この exact event / exact failure profile が generic な \(\texttt{ExpectationLowerConcentration}\) に packaged され、

\[
\begin{aligned}
\texttt{thresholdCrossingWithExactFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{collapseWithExactFailureBound\_of\_expectedMargin},
\end{aligned}
\]

\[
\begin{aligned}
\texttt{stoppedCollapseWithExactFailureBound\_of\_expectedMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithExactFailureBound\_of\_expectedMargin}
\end{aligned}
\]

が actual non-flat SAT process に対して直接成立する。さらに

\[
\texttt{thresholdCrossingWithExactFailureBound\_of\_activeLinearMargin}
\]

以下の active-prefix wrapper により、既に得られている exact linear center

\[
s_0 + n\log(8/7)
\]

を exact failure profile つきの fixed-time / stopped / hitting-time 境界へそのまま流せる。 したがって SAT 側では、Azuma / Chernoff のような closed-form tail bound をまだ導入しなくても、 realized fluctuation 自体が generic concentration / collapse interface の上で独立に立っている。

\section{さらに、}\label{ux3055ux3089ux306b-20}

\[
\texttt{Survival.SATStateDependentTailUpperBound}
\]

では、exact lower-tail concentration と generic Azuma wrapper のあいだに、 \textbf{「残る仕事は exact failure profile の closed-form 上界だけである」} ことを明示する bridge 層が追加された。ここでは

\[
\texttt{HasAzumaFailureUpperBound}
\]

が

\[
\texttt{exactFailureBound} \le \texttt{satAzumaFailureBound}
\]

という SAT 固有 tail inequality を抽象化し、

\[
\texttt{lowerTailWitness\_of\_hasAzumaFailureUpperBound}
\]

により、そのような inequality が一度証明されれば、exact lower-tail event から generic \(\texttt{StepModelLowerTailWitness}\) が自動生成されることが示される。さらに

\[
\texttt{stepModelAzumaData\_of\_hasAzumaFailureUpperBound}
\]

により actual non-flat SAT process が concrete な \texttt{StepModelAzumaData} に持ち上がり、

\[
\texttt{stoppedCollapseWithFailureBound\_of\_expectedMargin\_of\_hasAzumaFailureUpperBound},
\]

\[
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_expectedMargin\_of\_hasAzumaFailureUpperBound}
\]

および active-prefix 版 wrapper により、SAT 固有の tail inequality から collapse / hitting-time 高確率境界までが自動的に接続される。加えて

\[
\begin{aligned}
\texttt{incrementBound\_eq\_eightMulRandom3ClauseDrift}, \\
\qquad \\
\texttt{varianceProxy\_eq}
\end{aligned}
\]

により、この non-flat SAT process で用いられる bounded increment と分散上界量も 明示式

\[
8\log(8/7),\qquad n\cdot(8\log(8/7))^2
\]

として固定される。したがって、SAT 側で残る解析課題は、もはや concentration / collapse stack 全体ではなく、 \texttt{exactFailureBound} をこの explicit profile で抑える tail inequality そのものに局所化されている。

\section{さらに、}\label{ux3055ux3089ux306b-21}

\[
\texttt{Survival.SATStateDependentCountReduction}
\]

では、non-flat SAT clause-exposure process の exact lower-tail event を、active prefix 上の unsatisfied-clause count によって書き直す reduction 層が追加された。ここでは

\[
\texttt{unsatCountPrefix}
\]

を導入し、

\[
\texttt{cumulativeTotalProductionRV\_eq\_initial\_add\_unsatCount}
\]

により

\[
\Sigma_{n}(\tau)=s_0+\#\{\text{unsat up to }n\}\cdot 8\log(8/7)
\]

を pathwise に示す。その結果、

\[
\texttt{exactLowerTailEvent\_eq\_exactCountLowerTailEvent}
\]

と

\[
\texttt{exactFailureBound\_eq\_exactCountFailureBound}
\]

により、active prefix の exact SAT lower-tail failure profile は unsatisfied-count tail の問題へ還元される。特に

\[
\texttt{exactFailureBound\_eq\_of\_activePrefix}
\]

から、active prefix では exact failure profile は初期オフセット \(s_0\) に依存しない。したがって残る closed-form concentration の仕事は、SAT path space 全体ではなく、unsatisfied-count の binomial tail 上界に局所化された。

\section{さらに、}\label{ux3055ux3089ux306b-22}

\[
\texttt{Survival.SATStateDependentCountTailUpperBound}
\]

では、この count reduction を generic な高確率 bridge に持ち上げた。ここでは

\[
\texttt{HasCountFailureUpperBound}
\]

を

\[
\texttt{exactCountFailureBound} \le B(n,r)
\]

という active-prefix count tail の上界仮定として定式化し、

\[
\texttt{exactFailureBound\_le\_of\_hasCountFailureUpperBound}
\]

により actual non-flat SAT process の exact tail へ戻す。その上で

\[
\texttt{thresholdCrossingWithFailureBound\_of\_activeLinearMargin\_of\_hasCountFailureUpperBound},
\]

\[
\texttt{stoppedCollapseWithFailureBound\_of\_activeLinearMargin\_of\_hasCountFailureUpperBound},
\]

\[
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_activeLinearMargin\_of\_hasCountFailureUpperBound}
\]

により、count-tail の closed-form 上界さえあれば、actual non-flat SAT process の threshold crossing / collapse / hitting-time 境界が自動的に修復される。さらに

\[
\texttt{HasCountAzumaFailureUpperBound}
\]

という specialization により、この bridge はそのまま SAT の Azuma/Hoeffding profile にも接続される。 したがって残る解析課題は、もはや path-space 上の concentration 全体ではなく、unsatisfied-count の binomial / Chernoff tail inequality を証明することそのものに尽きる。

\section{さらに、}\label{ux3055ux3089ux306b-23}

\[
\texttt{Survival.SATStateDependentCountThreshold}
\]

では、count reduction を Chernoff / binomial tail に直接差し込める threshold 形式に正規化した。ここでは

\[
\texttt{unsatEmissionScale}=8\log(8/7)
\]

を導入し、

\[
\texttt{countThreshold}(n,r)=\frac{n\log(8/7)-r}{8\log(8/7)}
\]

を定義する。その上で

\[
\texttt{exactCountLowerTailEvent\_eq\_countThresholdEvent}
\]

により exact count event を

\[
\texttt{countThreshold} \le \#\{\text{unsat up to }n\}
\]

という canonical threshold event に書き換え、

\[
\texttt{compl\_countThresholdEvent\_eq\_countBelowThresholdEvent}
\]

と

\[
\texttt{exactCountFailureBound\_eq\_countBelowThresholdMeasure}
\]

により failure profile を

\[
\#\{\text{unsat up to }n\} < \texttt{countThreshold}(n,r)
\]

という lower-tail 事象の測度として表した。さらに

\[
\texttt{exactFailureBound\_eq\_countBelowThresholdMeasure}
\]

により、actual non-flat SAT process の active-prefix exact failure profile そのものが、この canonical count-threshold tail の問題であることが明示された。これで残る解析課題は、count tail の closed-form 上界、特に binomial / Chernoff inequality の証明に完全に局所化された。

\section{さらに、}\label{ux3055ux3089ux306b-24}

\[
\texttt{Survival.SATStateDependentCountSupportBound}
\]

では、この canonical threshold 形式の支持範囲を exact に切り出した。具体的には、

\[
r \ge n\log(8/7)
\]

なら exact failure profile はちょうど \(0\) になり、逆に

\[
r < -7n\log(8/7)
\]

なら exact failure profile はちょうど \(1\) になることを示している。したがって genuinely 非自明なのはこの両極端のあいだの interior regime のみであり、そこに対して binomial / Chernoff tail を入れれば、SAT の closed-form high-probability bound が閉じる。

\section{さらに、}\label{ux3055ux3089ux306b-25}

\[
\texttt{Survival.SATStateDependentCountSupportClippedUpperBound}
\]

では、この exact support envelope を任意の interior upper profile に合成する reusable layer を追加した。ここでは

\[
\texttt{supportClippedFailureBound}(B)
\]

を導入し、

\[
\begin{aligned}
r < -7n\log(8/7) \Rightarrow 1, \\
\qquad \\
n\log(8/7) \le r \Rightarrow 0, \\
\qquad \\
\text{interior} \Rightarrow B(n,r)
\end{aligned}
\]

という piecewise closed-form profile を定義する。その上で

\[
\texttt{hasCountFailureUpperBound\_supportClipped}
\]

により、count tail に対する任意の upper bound \(B\) は SAT tail の exact support によって自動的に sharpen されることを示した。さらに

\[
\texttt{satSupportClippedAzumaFailureBound}
\]

という specialization により、Azuma/Hoeffding の interior profile も、SAT の exact exterior support を組み込んだより鋭い piecewise profile に持ち上がる。したがって、将来 Chernoff や binomial tail を入れる場合も、analytic な改善は interior regime だけを書き換えればよい。

\section{さらに、}\label{ux3055ux3089ux306b-26}

\[
\texttt{Survival.SATStateDependentCountMarkovUpperBound}
\]

では、interior regime に対する最初の genuinely explicit な closed-form bound を Markov inequality から与えた。ここでは

\[
\texttt{unsatShortfallPrefix}=n-\#\{\text{unsat up to }n\}
\]

を導入し、

\[
\begin{aligned}
\mathbb E[\#\{\text{unsat up to }n\}]&=n/8, \\
\qquad \\
\mathbb E[\texttt{unsatShortfallPrefix}]&=7n/8
\end{aligned}
\]

を actual clause-exposure path measure 上で示している。その上で

\[
\texttt{countMarkovFailureBound}
\]

を

\[
\begin{aligned}
\Pr(\#\{\text{unsat up to }n\}<\texttt{countThreshold}(n,r)) \\
\;\le\; \\
\frac{(7n/8)}{\,n-\texttt{countThreshold}(n,r)\,}
\end{aligned}
\]

という interior profile として定義し、

\[
\texttt{exactCountFailureBound\_le\_countMarkovFailureBound}
\]

により exact count tail を explicit upper bound で支配した。さらに

\[
\texttt{satSupportClippedCountMarkovFailureBound}
\]

を通じて、この Markov interior profile を SAT の exact support envelope で sharpen し、actual non-flat SAT process に対する

\[
\begin{aligned}
\texttt{thresholdCrossingWithFailureBound\_of\_activeLinearMargin}, \\
\quad \\
\texttt{stoppedCollapseWithFailureBound\_of\_activeLinearMargin}, \\
\quad \\
\texttt{hittingTimeBeforeHorizonWithFailureBound\_of\_activeLinearMargin}
\end{aligned}
\]

まで直接流している。これは Chernoff / binomial tail よりは弱いが、actual non-flat clause-exposure process に対する最初の closed-form high-probability upper layer を与える。

\section{さらに、}\label{ux3055ux3089ux306b-27}

\[
\texttt{Survival.SATStateDependentCountChernoffUpperBound}
\]

では、SAT の count-threshold tail に対する Chernoff / binomial closed-form candidate を、そのまま actual process の collapse API に流せる形で切り出した。ここでは

\[
\begin{aligned}
\texttt{bernoulliRelativeEntropyCandidate}(q,p) \\
&= \\
q\log(q/p)+(1-q)\log((1-q)/(1-p))
\end{aligned}
\]

を導入し、

\[
\texttt{countThresholdRatio}(n,r)=\texttt{countThreshold}(n,r)/n
\]

を通じて、unsatisfied-count lower tail に対する rate candidate

\[
\texttt{countChernoffRate}(n,r)
\]

と failure profile

\[
\texttt{countChernoffFailureBound}(n,r)=\exp(-\texttt{countChernoffRate}(n,r))
\]

を定義している。その上で

\[
\texttt{HasCountChernoffFailureUpperBound}
\]

を唯一の残存仮定として、

\[
\texttt{satSupportClippedCountChernoffFailureBound}
\]

を exact support envelope と合成し、actual non-flat SAT process に対する

\[
\texttt{thresholdCrossingWithChernoffBound\_of\_activeLinearMargin},
\]

\[
\texttt{stoppedCollapseWithChernoffBound\_of\_activeLinearMargin},
\]

\[
\texttt{hittingTimeBeforeHorizonWithChernoffBound\_of\_activeLinearMargin}
\]

まで直接出せるようにした。したがって、SAT 固有の残る解析課題は、exact count tail がこの Chernoff candidate によって支配されることの証明そのものだけに局所化された。

\section{さらに、}\label{ux3055ux3089ux306b-28}

\[
\texttt{Survival.SATStateDependentCountChernoffMGF}
\]

では、mathlib の一般 Chernoff inequality を使い、SAT の actual count tail を MGF 形の Chernoff 上界へ直接押し込んだ。具体的には

\[
\texttt{unsatCountRV}(N,n)(\tau)=\#\{\text{unsat up to }n\}
\]

を定義し、

\[
\texttt{exactCountFailureBound\_le\_mgfChernoff}
\]

により

\[
\begin{aligned}
\Pr(\#\{\text{unsat up to }n\}<\texttt{countThreshold}(n,r)) \\
\le \\
\exp(-t\,\texttt{countThreshold}(n,r))\, \\
\mathbb E[\exp(t\,\texttt{unsatCountRV})] \\
\quad (t\le 0)
\end{aligned}
\]

を Lean 上で示した。その上で

\[
\texttt{bernoulliUnsatMGF}(t)=7/8+(1/8)e^t
\]

を導入し、

\[
\texttt{HasBernoulliMGFUpperBound}
\]

を「unsat count の MGF が Bernoulli 和の MGF で抑えられる」という残存仮定として切り出した。この仮定の下で

\[
\texttt{satSupportClippedClosedMGFChernoffFailureBound}
\]

を通じ、

\[
\texttt{thresholdCrossingWithClosedMGFChernoffBound\_of\_activeLinearMargin},
\]

\[
\texttt{stoppedCollapseWithClosedMGFChernoffBound\_of\_activeLinearMargin},
\]

\[
\texttt{hittingTimeBeforeHorizonWithClosedMGFChernoffBound\_of\_activeLinearMargin}
\]

まで actual non-flat SAT process へ直接接続している。これにより、Chernoff 証明の残りは KL 形の最適化より一段手前の、Bernoulli-sum MGF 境界の証明へと分解された。

\section{さらに、}\label{ux3055ux3089ux306b-29}

\[
\texttt{Survival.SATStateDependentCountMGFProduct}
\]

では、上の残存仮定

\[
\texttt{HasBernoulliMGFUpperBound}
\]

を actual recursive path PMF から直接構成した。ここではまず

\[
\texttt{unsatIndicator}
\]

を導入し、

\[
\texttt{oneStepUnsatIndicatorMGF\_eq\_bernoulliUnsatMGF}
\]

により one-step law \(\texttt{clausePMF}\) 上で

\[
\begin{aligned}
\mathbb E[e^{t I_{\mathrm{unsat}}}] \\
&= \\
7/8+(1/8)e^t
\end{aligned}
\]

を証明する。さらに

\[
\begin{aligned}
\texttt{unsatCountPrefix\_snoc\_of\_le}, \\
\qquad \\
\texttt{unsatCountPrefix\_snoc\_last}
\end{aligned}
\]

により、recursive path constructor \(\texttt{snoc}\) が old prefix count を保存し、terminal count だけを one-step indicator で増加させることを示す。その結果、

\[
\texttt{mgf\_unsatCountRV\_eq\_bernoulliUnsatMGF\_pow}
\]

により active prefix 上で

\[
\begin{aligned}
\mathbb E[\exp(t\,\#\{\text{unsat up to }n\})] \\
&= \\
\left(7/8+(1/8)e^t\right)^n
\end{aligned}
\]

が actual path measure 上で証明される。最後に

\[
\texttt{hasBernoulliMGFUpperBound\_pathPMF}
\]

が \texttt{SATStateDependentCountChernoffMGF} の closed MGF Chernoff wrapper に必要だった MGF witness を自動生成する。したがって SAT の non-flat clause-exposure process では、 exact count tail から MGF Chernoff tail、さらに support-clipped collapse / hitting-time bound までが、 外部の independence 仮定を追加せず actual path PMF の再帰だけから閉じた。

\section{さらに、}\label{ux3055ux3089ux306b-30}

\[
\texttt{Survival.SATStateDependentClosedMGFChernoff}
\]

では、前項で内部導出された

\[
\texttt{hasBernoulliMGFUpperBound\_pathPMF}
\]

を \texttt{SATStateDependentCountChernoffMGF} の Chernoff wrapper に実際に差し込み、closed MGF Chernoff profile を外部仮定なしで使える形にした。具体的には

\[
\texttt{hasCountFailureUpperBound\_closedMGFChernoff\_pathPMF}
\]

により

\[
\begin{aligned}
\texttt{exactCountFailureBound} \\
\le \\
\texttt{countClosedMGFChernoffFailureBound}(t) \\
\qquad (t\le 0)
\end{aligned}
\]

が actual SAT path PMF から直接導かれ、

\[
\texttt{hasCountFailureUpperBound\_supportClippedClosedMGFChernoff\_pathPMF}
\]

により exact support envelope と合成された sharpened profile も得られる。その上で

\[
\texttt{thresholdCrossingWithClosedMGFChernoffBound\_pathPMF},
\]

\[
\texttt{collapseWithClosedMGFChernoffBound\_pathPMF},
\]

\[
\texttt{stoppedCollapseWithClosedMGFChernoffBound\_pathPMF},
\]

\[
\texttt{hittingTimeBeforeHorizonWithClosedMGFChernoffBound\_pathPMF}
\]

が追加され、actual non-flat SAT clause-exposure process に対して、任意の \(t\le 0\) の closed MGF Chernoff failure profile から fixed-time / collapse / stopping-time / hitting-time 境界までが直接出る。 したがって SAT chain は、expected drift、exact support、MGF product、Chernoff application、collapse wrapper の全てを random 3-SAT の actual clause-exposure path measure から自己完結的に接続した。

\section{さらに、}\label{ux3055ux3089ux306b-31}

\[
\texttt{Survival.SATStateDependentCountChernoffKL}
\]

では、closed MGF Chernoff family を KL/relative-entropy rate へ落とすための optimization-facing layer が追加された。まず Bernoulli lower-tail の標準的な exponential tilt

\[
\begin{aligned}
\texttt{bernoulliLowerTailTilt}(q,p) \\
&= \\
\log\frac{q(1-p)}{p(1-q)}
\end{aligned}
\]

を導入し、SAT の unsatisfied probability \(p=1/8\) に特殊化した

\[
\texttt{satLowerTailTilt}
\]

を定義する。ただし Lean 上で全パラメータに対する total な failure profile として扱うため、

\[
\texttt{clippedSatLowerTailTilt}(q)=\min(0,\texttt{satLowerTailTilt}(q))
\]

として常に lower-tail Chernoff に admissible な \(t\le 0\) を選ぶ。その結果、

\[
\texttt{countOptimizedClosedMGFChernoffFailureBound}
\]

は count threshold ratio ごとに自動的に tilt を選んだ optimized closed-MGF profile となり、

\[
\texttt{hasCountFailureUpperBound\_optimizedClosedMGF\_pathPMF}
\]

により actual SAT count tail の上界であることが示される。さらに support envelope と合成した

\[
\texttt{satSupportClippedOptimizedClosedMGFChernoffFailureBound}
\]

から

\[
\texttt{thresholdCrossingWithOptimizedClosedMGFBound\_pathPMF},
\]

\[
\texttt{collapseWithOptimizedClosedMGFBound\_pathPMF},
\]

\[
\texttt{stoppedCollapseWithOptimizedClosedMGFBound\_pathPMF},
\]

\[
\texttt{hittingTimeBeforeHorizonWithOptimizedClosedMGFBound\_pathPMF}
\]

が得られる。したがって SAT chain は、固定 \(t\) の Chernoff family から、threshold に応じて tilt を自動選択する optimized MGF Chernoff bound へ進んだ。

同時にこのファイルでは、教科書的な KL rate candidate

\[
\texttt{countChernoffFailureBound}
\]

への純代数的橋として

\[
\texttt{HasOptimizedMGFToKLBound}
\]

を切り出している。これは optimized MGF profile が \(\exp(-nD(q\|1/8))\) 型の candidate で支配されるという実変数不等式であり、これが証明されれば

\[
\texttt{hasCountChernoffFailureUpperBound\_of\_optimizedMGFToKL}
\]

により、既存の KL/Chernoff wrapper

\[
\begin{aligned}
\texttt{stoppedCollapseWithChernoffBound\_of\_activeLinearMargin}, \\
\qquad \\
\texttt{hittingTimeBeforeHorizonWithChernoffBound\_of\_activeLinearMargin}
\end{aligned}
\]

が外部確率仮定なしで得られる。この時点で、残る仕事は確率過程側ではなく、Bernoulli Chernoff の one-variable KL 最適化という純解析補題に局所化された。次項では、この局所化をさらに margin-aware な形に補正したうえで、必要な interior KL 代数を Lean 上で閉じる。

\section{ただし、KL lower-tail rate を全ての deviation budget (r) にそのまま適用するのは強すぎる。}\label{ux305fux3060ux3057kl-lower-tail-rate-ux3092ux5168ux3066ux306e-deviation-budget-r-ux306bux305dux306eux307eux307eux9069ux7528ux3059ux308bux306eux306fux5f37ux3059ux304eux308b}

特に \(r<0\) では count threshold が平均より上側に動くため、これは下側偏差の large-deviation regime ではない。そこで

\[
\texttt{Survival.SATStateDependentCountChernoffKLAlgebra}
\]

では、KL 出口を数学的に安全な形に補正するため、

\[
\texttt{nonnegativeMarginSupportClippedFailureBound}
\]

を導入した。この profile は \(r<0\) では自明な failure bound \(1\) を返し、\(0\le r\) の場合だけ 既存の support-clipped KL/Chernoff profile に委ねる。さらに support envelope の外側は Lean 側で すでに処理されるため、必要な純解析補題は

\[
\texttt{HasInteriorOptimizedMGFToKLBound}
\]

として

\[
\begin{aligned}
0\le r \\
\quad\wedge\quad \\
r < n\,\texttt{random3ClauseDrift} \\
\quad\Longrightarrow\quad \\
\texttt{countOptimizedClosedMGFChernoffFailureBound}(n,r) \\
\le \\
\texttt{countChernoffFailureBound}(n,r)
\end{aligned}
\]

に局所化される。これは、count threshold ratio が \(0<q\le1/8\) に入る genuine lower-tail interior だけを KL 最適化の対象にする、という補正である。

またこのファイルでは、

\[
\begin{aligned}
\texttt{countThresholdRatio\_pos\_of\_nonneg\_of\_lt\_activeDrift}, \\
\qquad \\
\texttt{countThresholdRatio\_le\_unsatProb\_of\_nonneg}
\end{aligned}
\]

により、\(0\le r<n\log(8/7)\) から \(0<q\le1/8\) が導かれることを形式化している。したがって

\[
\texttt{countOptimizingTilt\_eq\_unclipped\_of\_interior}
\]

により、この interior では clipped tilt が通常の Bernoulli lower-tail tilt に一致する。

さらに one-variable KL 代数そのものも Lean 上で証明されている。具体的には

\[
\begin{aligned}
\texttt{bernoulliUnsatMGF\_satLowerTailTilt}, \\
\qquad \\
\texttt{satLowerTailTilt\_log\_mgf\_identity}
\end{aligned}
\]

により最適 tilt での Bernoulli MGF を閉形式化し、 \texttt{optimizedClosedMGFReal\_eq\_exp\_neg\_bernoulliRelativeEntropy} で optimized closed-MGF profile が \(\exp(-nD(q\|1/8))\) に一致することを示す。加えて

\[
\begin{aligned}
\texttt{bernoulliRelativeEntropyCandidate\_nonneg}, \\
\qquad \\
\texttt{countChernoffRate\_eq\_of\_interior}
\end{aligned}
\]

により KL rate の非負性と count-level rate の同一性を処理し、最終的に

\[
\texttt{countOptimizedClosedMGFChernoffFailureBound\_eq\_countChernoffFailureBound\_of\_interior}
\]

および

\[
\texttt{hasInteriorOptimizedMGFToKLBound}
\]

が得られる。したがって、前項で残っていた one-variable KL 最適化補題は、必要な interior regime では 仮定ではなく定理になった。

この補正と KL 代数のもとで、

\[
\texttt{hasCountFailureUpperBound\_nonnegativeMarginSupportClippedChernoff\_pathPMF}
\]

が証明され、さらに

\[
\texttt{thresholdCrossingWithNonnegativeMarginChernoffBound\_pathPMF},
\]

\[
\texttt{collapseWithNonnegativeMarginChernoffBound\_pathPMF},
\]

\[
\texttt{stoppedCollapseWithNonnegativeMarginChernoffBound\_pathPMF},
\]

\[
\texttt{hittingTimeBeforeHorizonWithNonnegativeMarginChernoffBound\_pathPMF}
\]

が得られる。つまり、SAT の KL/Chernoff 出口は、負の margin で過大な KL rate を主張しない support-aware / margin-aware な形に修正されたうえで、\(0\le r<n\log(8/7)\) の genuine lower-tail KL 最適化補題も Lean 上で discharge された。

\section{SAT 固有の (1/8) に依存していた Chernoff-KL 代数は、水平展開に向けて}\label{sat-ux56faux6709ux306e-18-ux306bux4f9dux5b58ux3057ux3066ux3044ux305f-chernoff-kl-ux4ee3ux6570ux306fux6c34ux5e73ux5c55ux958bux306bux5411ux3051ux3066}

\[
\texttt{Survival.BernoulliCSPTemplate}
\]

として抽象化された。このファイルでは、bad event 確率 \(p\in(0,1)\) を持つ one-sided Bernoulli CSP exposure に対して、

\[
\begin{aligned}
\texttt{bernoulliBadMGF}, \\
\qquad \\
\texttt{bernoulliLowerTailTilt}, \\
\qquad \\
\texttt{bernoulliRelativeEntropy}
\end{aligned}
\]

を定義し、\texttt{bernoulliBadMGF\_lowerTailTilt}, \texttt{bernoulliLowerTailTilt\_log\_mgf\_identity}, \texttt{optimizedClosedMGFReal\_eq\_exp\_neg\_bernoulliRelativeEntropy} により、 MGF 最適化から KL rate が出る one-variable 代数を \(p\) 一般で証明している。

さらに

\[
\texttt{Parameters}
\]

により、平均 drift

\[
\texttt{drift}=\log\frac{1}{1-p}
\]

と one-sided emission scale

\[
\texttt{badEmissionScale}=\frac{\texttt{drift}}{p}
\]

をまとめ、\(\mathbb E[\text{one-step emission}]=\texttt{drift}\) を

\[
\texttt{expectedBadEmission\_eq\_drift}
\]

として証明している。この抽象 layer では、threshold ratio、clipped tilt、optimized closed-MGF profile、 および KL/Chernoff failure profile が \(p\) 一般で定義され、interior regime では

\[
\texttt{optimizedClosedMGFReal\_failure\_eq\_chernoffFailureBound\_of\_interior}
\]

が成立する。最後に

\[
\texttt{random3SATParameters}
\]

を置き、

\[
\begin{aligned}
\texttt{random3SATParameters\_drift\_eq\_log}, \\
\qquad \\
\texttt{random3SATParameters\_badEmissionScale\_eq}
\end{aligned}
\]

で \(p=1/8\) が従来の random 3-SAT の drift \(\log(8/7)\) と emission scale \(8\log(8/7)\) に戻ることを確認している。したがって、SAT chain は単発の特殊例ではなく、 \(k\)-SAT や他の Bernoulli 型 CSP へ移植できる抽象テンプレートを持つ形になった。

\section{この抽象テンプレートは、}\label{ux3053ux306eux62bdux8c61ux30c6ux30f3ux30d7ux30ecux30fcux30c8ux306f}

\[
\texttt{Survival.KSATBernoulliTemplate}
\]

で random \(k\)-SAT に特殊化された。固定割当のもとで一様ランダムな \(k\)-clause が falsify される確率を

\[
\texttt{kSATBadProb}(k)=(1/2)^k
\]

と定義し、\(k>0\) なら

\[
\begin{aligned}
\texttt{kSATBadProb\_pos}, \\
\qquad \\
\texttt{kSATBadProb\_lt\_one}
\end{aligned}
\]

により、これが Bernoulli-CSP parameter として有効であることを示している。これにより

\[
\texttt{kSATParameters}(k)
\]

が得られ、対応する drift

\[
\texttt{kSATDrift}(k)=\log\frac{1}{1-(1/2)^k}
\]

と one-sided emission scale

\[
\texttt{kSATBadEmissionScale}(k)
\]

が定義される。さらに

\[
\texttt{kSAT\_expectedBadEmission\_eq\_drift}
\]

により、bad clause だけが emission を持つ非平坦モデルでも、期待一歩生成量が drift に一致することが \(k\) 一般で証明されている。

また、generic template の KL/Chernoff identity は

\[
\texttt{kSAT\_optimizedClosedMGFReal\_failure\_eq\_chernoffFailureBound\_of\_interior}
\]

として random \(k\)-SAT に引き継がれる。最後に

\[
\texttt{kSAT3Parameters}
\]

を置き、

\[
\begin{aligned}
\texttt{kSAT3Parameters\_badProb\_eq}, \\
\qquad \\
\texttt{kSAT3Parameters\_drift\_eq\_random3SATParameters}, \\
\qquad \\
\texttt{kSAT3Parameters\_badEmissionScale\_eq\_random3SATParameters}
\end{aligned}
\]

により、\(k=3\) が既存の random 3-SAT chain と同じ bad probability、drift、emission scale に戻ることを 確認している。これにより、SAT の Chernoff-KL stack は 3-SAT 固有の完成形から、random \(k\)-SAT family の Lean 検証テンプレートへ拡張された。

\section{さらに}\label{ux3055ux3089ux306b-32}

\[
\texttt{Survival.BernoulliCSPToSATBridge}
\]

では、generic Bernoulli-CSP template の \(p=1/8\) 特殊化が、既存の random 3-SAT stack と同じ対象を 記述していることを明示した。具体的には

\[
\begin{aligned}
\texttt{random3SATParameters\_drift\_eq\_random3ClauseDrift}, \\
\qquad \\
\texttt{random3SATParameters\_badEmissionScale\_eq\_unsatEmissionScale}
\end{aligned}
\]

により、generic 側の drift / emission scale が既存の SAT 定数と一致することを示す。また

\[
\begin{aligned}
\texttt{random3SATParameters\_bernoulliBadMGF\_eq\_bernoulliUnsatMGF}, \\
\qquad \\
\texttt{random3SATParameters\_lowerTailTilt\_eq\_satLowerTailTilt}, \\
\qquad \\
\texttt{random3SATParameters\_relativeEntropy\_eq\_candidate}
\end{aligned}
\]

により、MGF、lower-tail tilt、KL candidate も既存 SAT 側の定義と一致することが証明されている。

さらに、count reduction 側との対応として

\[
\begin{aligned}
\texttt{random3SATParameters\_countThreshold\_eq\_countThreshold}, \\
\quad \\
\texttt{random3SATParameters\_thresholdRatio\_eq\_countThresholdRatio}, \\
\quad \\
\texttt{random3SATParameters\_optimizingTilt\_eq\_countOptimizingTilt}
\end{aligned}
\]

が置かれ、最終的に

\[
\begin{aligned}
\texttt{random3SATParameters\_chernoffFailureBound\_eq\_countChernoffFailureBound}, \\
\qquad \\
\texttt{random3SATParameters\_optimizedClosedMGFReal\_eq\_countOptimizedClosedMGF}
\end{aligned}
\]

が得られる。したがって、generic template から得られる interior KL identity は

\[
\texttt{random3SATParameters\_generic\_failure\_eq\_countChernoffFailureBound\_of\_interior}
\]

として、既存の 3-SAT Chernoff/KL failure profile へそのまま戻る。この bridge により、3-SAT で先に構築した concrete stack と、後から抽象化した Bernoulli-CSP / \(k\)-SAT stack が分岐していないことが Lean 上で 確認された。

\section{これに加えて}\label{ux3053ux308cux306bux52a0ux3048ux3066}

\[
\texttt{Survival.BernoulliCSPPathMeasure}
\]

では、Bernoulli-CSP template に対する実際の有限 horizon path-space を抽象化した。すなわち、bad event probability \(p\) を持つ二値 outcome

\[
\texttt{Outcome.good},\qquad \texttt{Outcome.bad}
\]

の i.i.d. exposure path

\[
\texttt{pathPMF}(P,N),\qquad \texttt{pathMeasure}(P,N)
\]

を定義し、active prefix 上の bad-count

\[
\texttt{badCountPrefix},\qquad \texttt{badCountRV}
\]

を導入している。中心となる定理は

\[
\texttt{mgf\_badCountRV\_eq\_bernoulliBadMGF\_pow}
\]

であり、

\[
\begin{aligned}
\mathbb{E}\exp(t\,\#\mathrm{bad}_{n}) \\
&= \\
\bigl((1-p)+p e^{t}\bigr)^n
\end{aligned}
\]

を、path PMF の再帰構造そのものから証明する。さらに

\[
\texttt{hasBernoulliMGFUpperBound\_pathPMF}
\]

により、Chernoff/KL stack に必要な MGF witness が外部仮定ではなく、generic path measure から直接生成される。

\section{その (k)-SAT 特殊化として}\label{ux305dux306e-k-sat-ux7279ux6b8aux5316ux3068ux3057ux3066}

\[
\texttt{Survival.KSATClauseExposureProcess}
\]

を置いた。ここでは

\[
p_{k}=(1/2)^k
\]

を bad-clause probability とする random \(k\)-SAT clause-exposure path

\[
\texttt{pathPMF}(k,N),\qquad \texttt{pathMeasure}(k,N)
\]

を generic Bernoulli-CSP path measure から定義し、

\[
\texttt{mgf\_badCountRV\_eq\_kSATBadMGF\_pow}
\]

として

\[
\begin{aligned}
\mathbb{E}\exp(t\,\#\mathrm{bad}_{n}) \\
&= \\
\bigl(\texttt{kSATBadMGF}(k,t)\bigr)^n
\end{aligned}
\]

を証明している。さらに

\[
\texttt{threeSAT\_kSATBadMGF\_eq}
\]

により、\(k=3\) では

\[
\texttt{kSATBadMGF}(3,t)=\frac78+\frac18 e^t
\]

に戻る。これにより、3-SAT で完成した actual path-space + MGF product の構造が、random \(k\)-SAT family へそのまま移植できることが Lean 上で確認された。

\section{次に}\label{ux6b21ux306b}

\[
\texttt{Survival.BernoulliCSPPathChernoff}
\]

では、generic Bernoulli-CSP path measure と Bernoulli-CSP template の KL 代数を接続した。まず

\[
\begin{aligned}
\texttt{countBelowThresholdEvent},\qquad \\
\texttt{exactCountFailureBound}
\end{aligned}
\]

を定義し、actual path measure 上の bad-count lower tail を明示する。そのうえで

\[
\texttt{exactCountFailureBound\_le\_mgfChernoff}
\]

により、任意の \(t\le 0\) について MGF Chernoff bound を得る。さらに path measure から導出済みの MGF 積

\[
\texttt{mgf\_badCountRV\_eq\_bernoulliBadMGF\_pow}
\]

を代入し、

\[
\texttt{hasCountFailureUpperBound\_closedMGF\_pathPMF}
\]

として closed MGF profile が実際の count-tail upper bound になることを証明する。

その後、template 側の最適 lower-tail tilt

\[
\texttt{optimizingTilt}
\]

を代入した

\[
\texttt{countOptimizedClosedMGFChernoffFailureBound}
\]

を置き、

\[
\texttt{exactCountFailureBound\_le\_chernoffFailureBound\_of\_interior}
\]

として、interior 条件

\[
0\le r < n\,\texttt{drift}
\]

のもとで KL/Chernoff failure profile

\[
\texttt{chernoffFailureBound}
\]

が actual path measure の lower-tail failure を支配することを示した。また

\[
\begin{aligned}
\texttt{cumulativeProduction},\qquad \\
\texttt{cumulativeLowerTailEvent}
\end{aligned}
\]

を定義し、

\[
\texttt{cumulativeLowerTailMeasure\_le\_chernoffFailureBound\_of\_interior}
\]

により、bad outcome に emission scale を割り当てた累積生成量の lower tail も同じ KL/Chernoff profile で 抑えられる。

\section{その (k)-SAT 版として}\label{ux305dux306e-k-sat-ux7248ux3068ux3057ux3066}

\[
\texttt{Survival.KSATChernoffCollapse}
\]

を追加した。このファイルでは

\[
\begin{aligned}
\texttt{countChernoffFailureBound}(k) \\
&= \texttt{chernoffFailureBound}(\texttt{kSATParameters}(k))
\end{aligned}
\]

を domain-facing な failure profile として置き、

\[
\texttt{exactCountFailureBound\_le\_chernoffFailureBound\_of\_interior}
\]

により、random \(k\)-SAT の actual clause-exposure path measure 上で bad-clause count の lower tail が KL/Chernoff bound に従うことを示す。さらに

\[
\texttt{cumulativeLowerTailMeasure\_le\_chernoffFailureBound\_of\_interior}
\]

により、one-sided bad-clause emission による累積生成量の lower tail も同じ profile で抑えられる。これは random 3-SAT 固有だった high-probability tendency/collapse bound を random \(k\)-SAT family へ持ち上げる 入口である。

\section{さらに}\label{ux3055ux3089ux306b-33}

\[
\texttt{Survival.BernoulliCSPPathCollapse}
\]

では、generic Bernoulli-CSP の lower-tail bound を operational collapse API に戻した。まず

\[
\texttt{stepEmission}
\]

を定義し、bad outcome だけが emission scale を持つ累積過程

\[
\texttt{process}(P,N,s_0)
\]

を実際の path measure 上の

\[
\texttt{StochasticExpectedProcess}
\]

として構成する。中心となる接続は

\[
\texttt{thresholdCrossingWithChernoffBound\_of\_linearMargin}
\]

であり、linear center margin

\[
-\log\theta \le \texttt{linearCenter}(P,s_0,n)-r
\]

と interior 条件 \(0\le r < n\,\texttt{drift}\) から、

\[
\texttt{ThresholdCrossingWithFailureBound}
\]

を KL/Chernoff profile

\[
\texttt{chernoffFailureBound}(n,r)
\]

で得る。さらに同じ event を用いて

\[
\begin{aligned}
\texttt{collapseWithChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{stoppedCollapseWithChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{hittingTimeBeforeHorizonWithChernoffBound\_of\_linearMargin}
\end{aligned}
\]

を導出し、generic Bernoulli-CSP path-space tail bound を fixed-time collapse、stopped collapse、hitting-time before horizon の operational statement に変換している。

\section{\texorpdfstring{(\texttt{Survival.KSATChernoffCollapse}) には、この generic operational layer の random (k)-SAT wrapper}{() には、この generic operational layer の random (k)-SAT wrapper}}\label{ux306bux306fux3053ux306e-generic-operational-layer-ux306e-random-k-sat-wrapper}

も追加した。すなわち

\[
\texttt{process}(k,N,s_0)
\]

を random \(k\)-SAT の one-sided bad-clause emission process として定義し、

\[
\begin{aligned}
\texttt{thresholdCrossingWithChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{collapseWithChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{stoppedCollapseWithChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{hittingTimeBeforeHorizonWithChernoffBound\_of\_linearMargin}
\end{aligned}
\]

を \(k\) 一般で得ている。これにより、

\[
\begin{aligned}
\text{random }k\text{-SAT definition} \\
\to \text{actual path-space} \\
\to \text{KL/Chernoff lower-tail} \\
\to \text{collapse / hitting-time bound}
\end{aligned}
\]

という operational pipeline が Lean 上で一本化された。

\section{最後に}\label{ux6700ux5f8cux306b}

\[
\texttt{Survival.KSATToSATChernoffBridge}
\]

では、\(k=3\) の wrapper が既存の random 3-SAT Chernoff/KL stack に戻ることを確認した。具体的には

\[
\begin{aligned}
\texttt{kSATParameters\_three\_eq\_random3SATParameters}, \\
\qquad \\
\texttt{kSAT3Drift\_eq\_random3ClauseDrift}, \\
\qquad \\
\texttt{kSAT3BadMGF\_eq\_bernoulliUnsatMGF}
\end{aligned}
\]

により、parameter、drift、MGF が既存 SAT 側と一致することを示す。また

\[
\begin{aligned}
\texttt{kSAT3\_chernoffFailureBound\_eq\_countChernoffFailureBound}, \\
\qquad \\
\texttt{kSAT3\_optimizedClosedMGF\_eq\_countOptimizedClosedMGF}
\end{aligned}
\]

によって、failure profile も既存の SAT count Chernoff profile と一致する。さらに

\[
\texttt{threeSAT\_cumulativeLowerTailMeasure\_le\_existingChernoffFailureBound\_of\_interior}
\]

として、\(k=3\) の cumulative lower-tail bound を既存 SAT の Chernoff/KL failure profile で直接読める形に 戻している。

加えて operational statement についても

\[
\begin{aligned}
\texttt{threeSAT\_thresholdCrossingWithExistingChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{threeSAT\_collapseWithExistingChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{threeSAT\_stoppedCollapseWithExistingChernoffBound\_of\_linearMargin}, \\
\quad \\
\texttt{threeSAT\_hittingTimeBeforeHorizonWithExistingChernoffBound\_of\_linearMargin}
\end{aligned}
\]

を置いた。これにより、\(k=3\) の generalized wrapper から得られる threshold / collapse / stopped-collapse / hitting-time bound も、既存 random 3-SAT の Chernoff/KL failure profile で読めることが確認された。したがって、 random \(k\)-SAT への一般化と、既存 random 3-SAT chain の完成形は Lean 上で分岐していない。

要するに、§4--§7 の数学的核は Lean 上で直接対応があり、§12 の目標定理群についても、目標定理 2--5 に対応する定義層・境界層・wrapper 層に加えて、構造粒度変換後の典型的非減少の主定理層と、deterministic / stochastic / finite-state Markov-style / finite-horizon actual Markov chain な concrete example 群まで形式化が進んでいる。他方、本文で「今後の課題」として残している自然測度、表現共変性、微視基礎、強い意味での構造粒度変換後の典型性は、Lean 側でもなお未完成の研究課題として対応している。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{13}
\tightlist
\item
  Freeze snapshot: SAT chain v1.0
\end{enumerate}

以上のうち、random 3-SAT / random \(k\)-SAT / Bernoulli bad-event CSP に関する finite-horizon chain は、 本補論の現時点では \textbf{SAT chain v1.0} として凍結する。凍結範囲は

\[
\begin{aligned}
\text{problem data} \\
\to \text{actual finite-horizon path measure} \\
\to \text{non-flat bad-outcome additive functional} \\
\to \text{MGF product} \\
\to \text{Chernoff/KL lower-tail profile} \\
\to \text{collapse / stopped-collapse / hitting-time bounds}
\end{aligned}
\]

である。対応する claim-to-theorem index は

\[
\texttt{lean/SAT\_CHAIN\_THEOREM\_MAP.md}
\]

に分離した。これにより、どの主張が外部仮定で、どの主張が Lean 上で導出済みで、どこが意図的な未着手 であるかを一枚の表として確認できる。

この freeze は、SAT chain の作業を打ち切るという意味ではなく、有限地平線・iid Bernoulli bad-event exposure というスコープで完成済みの core を固定するという意味である。したがって、無限 horizon （Ionescu-Tulcea 型構成）、almost-sure ergodic theorem、非 iid / adaptive clause selection、CDCL や WalkSAT の solver-adaptive dynamics、XOR-SAT の rank/nullity dynamics は、SAT chain v1.0 の外側に 意図的に置く。

次の水平展開では、まず固定割当のもとでの \(k\)-XOR-SAT bad-event exposure と、固定 coloring のもと での \(q\)-coloring edge exposure を Bernoulli-CSP template へ載せるのが自然である。前者は full rank dynamics ではなく、後者は random graph の依存構造や coloring algorithm の dynamics ではない。どちら も、Bernoulli bad-event CSP template の再利用性を検証する最小対象であり、SAT 固有ではない universality claim の準備として機能する。

この最小対象は Lean 側では

\[
\begin{aligned}
\texttt{Survival.XORSATBernoulliTemplate}, \\
\qquad \\
\texttt{Survival.XORSATClauseExposureProcess}, \\
\qquad \\
\texttt{Survival.XORSATChernoffCollapse}
\end{aligned}
\]

として追加された。ここでは固定割当のもとで、ランダムな XOR equation が bad になる確率を \(1/2\) として Bernoulli-CSP template に載せる。したがって、これは XOR-SAT の rank/nullity dynamics ではなく、 SAT chain v1.0 の水平展開可能性を確かめるための exposure-level instance である。

さらに、第二の水平展開として

\[
\begin{aligned}
\texttt{Survival.QColoringBernoulliTemplate}, \\
\qquad \\
\texttt{Survival.QColoringEdgeExposureProcess}, \\
\qquad \\
\texttt{Survival.QColoringChernoffCollapse}
\end{aligned}
\]

を追加した。ここでは固定 coloring のもとで、露出された edge が monochromatic になる確率を \(1/q\) と し、対応する drift を \(\log(q/(q-1))\) として Bernoulli-CSP template に載せる。これは、SAT/XOR-SAT の Boolean 系だけでなく、多値 CSP の exposure-level instance にも同じ Chernoff-KL collapse wrapper が 移植できることを示す。

これらを横断的に読むために、Lean 側には

\[
\texttt{Survival.BernoulliCSPUniversality}
\]

を追加した。この module は、domain-specific な bad-event probability \(p\in(0,1)\) を \texttt{Parameters} として与えるだけで、有限 path measure、MGF product、Chernoff/KL failure profile、 collapse / stopped-collapse / hitting-time wrapper が同じ形で得られることを一つの interface に束ねる。 対応表は \(\texttt{lean/BERNOULLI\_CSP\_UNIVERSALITY\_MAP.md}\) に分離した。

この interface の追加後、さらに固定割当 \(k\)-NAE-SAT exposure を

\[
\begin{aligned}
\texttt{Survival.NAESATBernoulliTemplate}, \\
\qquad \\
\texttt{Survival.NAESATClauseExposureProcess}, \\
\qquad \\
\texttt{Survival.NAESATChernoffCollapse}
\end{aligned}
\]

として追加した。NAE-SAT では、ランダムな signed \(k\)-clause が not-all-equal 条件を破る確率は \((1/2)^{k-1}\) であり、特に \(k=3\) では \(1/4\)、drift は \(\log(4/3)\) になる。これにより、 Boolean CSP 内でも SAT とは異なる bad-event rate を持つ例が同じ universality interface に載った。

さらに、finite-alphabet forbidden-pattern CSP の generic layer として

\[
\begin{aligned}
\texttt{Survival.ForbiddenPatternCSPTemplate}, \\
\qquad \\
\texttt{Survival.ForbiddenPatternCSPExposureProcess}, \\
\qquad \\
\texttt{Survival.ForbiddenPatternCSPChernoffCollapse}
\end{aligned}
\]

を追加した。これは、alphabet size を \(q\)、arity を \(k\)、forbidden local patterns の個数を \(m\) と したとき、bad-event probability を \(m/q^k\) として Bernoulli-CSP template に載せる層である。 対応する drift は \(\log(q^k/(q^k-m))\) となる。この層により、個別の CSP ごとに同じ collapse wrapper を再実装するのではなく、domain 固有の combinatorial witness が \(0<m<q^k\) を与えれば、有限 horizon の path measure、MGF product、Chernoff/KL bound、collapse / hitting-time bound が同じ interface から 得られる。

この generic layer の最初の具体 special case として、

\[
\texttt{Survival.HypergraphColoringChernoffCollapse}
\]

を追加した。これは、固定 \(q\)-coloring のもとで \(k\)-uniform hyperedge を iid に露出するモデルであり、 bad event は hyperedge が monochromatic になることである。局所 color pattern は \(q^k\) 個あり、そのうち monochromatic pattern は \(q\) 個なので、bad-event probability は \(q/q^k\)、drift は

\[
\log\frac{q^k}{q^k-q}
\]

になる。ただし、この層も固定 coloring の finite-prefix iid exposure であり、random hypergraph の次数相関、 overlapping hyperedge dependence、coloring algorithm dynamics は future work として切り分けている。 以上の横断層を、finite-horizon iid bad-event exposure に対する

\[
\text{Bernoulli CSP universality v1.2}
\]

として凍結した。

その後の拡張として、

\[
\texttt{Survival.MultiForbiddenPatternCSP}
\]

を追加した。これは、domain 固有の combinatorial witness を

\[
(q,k,m,\ 0<m<q^k)
\]

として受け取り、既存の forbidden-pattern CSP layer に渡す bridge である。この witness から bad-event probability \(m/q^k\)、drift \(\log(q^k/(q^k-m))\)、finite path measure、MGF product、 Chernoff/KL profile、collapse / stopped-collapse / hitting-time wrapper が生成される。特に hypergraph coloring は \(m=q\) の witness としても表現でき、直接 specialization と witness 経由の parameters が一致することを Lean 上で確認している。

さらに、

\[
\texttt{Survival.ExactlyOneSATChernoffCollapse}
\]

を追加した。固定 assignment のもとで random signed \(k\)-clause を露出すると、局所 truth pattern は \(2^k\) 個ある。exactly-one-SAT では、そのうち exactly one literal が true になる \(k\) 個の pattern だけが allowed であり、残りの \(2^k-k\) 個が forbidden である。したがって bad-event probability は

\[
\frac{2^k-k}{2^k}
\]

であり、対応する drift は

\[
\log\frac{2^k}{k}
\]

になる。この例は、\texttt{MultiForbiddenPatternCSP} が抽象 bridge に留まらず、domain 固有の forbidden-count witness から直接 Chernoff/KL collapse layer へ到達できることを示す。

さらにこの exactly-one 例を個別特化で終わらせず、

\[
\texttt{Survival.CardinalitySATChernoffCollapse}
\]

として exactly-\(r\)-of-\(k\) cardinality-SAT family へ一般化した。固定 assignment のもとで random signed \(k\)-clause を露出すると、\(2^k\) 個の局所 truth pattern のうち exactly \(r\) literals が true になる pattern は \(\binom{k}{r}\) 個である。したがって forbidden pattern は

\[
2^k-\binom{k}{r}
\]

個であり、bad-event probability は

\[
\frac{2^k-\binom{k}{r}}{2^k},
\]

対応する drift は

\[
\log\frac{2^k}{\binom{k}{r}}
\]

になる。Lean 側では \(\binom{k}{r}>0\) と \(\binom{k}{r}<2^k\) を \(0<k,\ r\le k\) から証明し、 同じ witness bridge に渡している。また

\[
\texttt{exactlyOneSAT\_eq\_exactRSAT}
\]

により、exactly-one-SAT はこの cardinality family の \(r=1\) specialization として接続される。

この cardinality family はさらに threshold 形式へ拡張できる。Lean 側では

\[
\texttt{Survival.ThresholdCardinalitySATChernoffCollapse}
\]

を追加し、at-most-\(r\)-of-\(k\) と at-least-\(r\)-of-\(k\) を同じ multi-forbidden witness bridge に 載せた。at-most-\(r\) の allowed pattern 数は

\[
\sum_{i=0}^{r} \binom{k}{i},
\]

at-least-\(r\) の allowed pattern 数は

\[
\sum_{i=r}^{k} \binom{k}{i}
\]

である。したがって drift は一般に

\[
\log\frac{2^k}{\text{allowed}}
\]

となる。Lean では、\(r<k\) のとき at-most 側の部分和が \(2^k\) より真に小さいこと、また \(0<r\le k\) のとき at-least 側の部分和が \(0\) と \(2^k\) の間に入ることを、二項係数和 \(\sum_{i=0}^{k}\binom{k}{i}=2^k\) と正の抜け項から証明している。これにより、 exactly-one / exactly-\(r\) / at-most-\(r\) / at-least-\(r\) が、同一の Bernoulli-CSP universality interface 上の cardinality-constraint family として扱える。

一方で、仕様固定構造層の検証を CSP 系だけに閉じないため、Lean 側には非CSPの教科書的 core example も追加した。 ただし、これらは各分野の本命定理を再証明するためのファイルではない。目的は、構造持続論の最小骨格が 有限 prefix の operational skeleton としてどこまで同じ形で走るかを固定することである。したがって、 これらは信頼性工学、材料疲労、待ち行列、通信路符号化、percolation などに対する新しい定量予測として 読むべきものではなく、既知の古典例を最小語彙で歪めず表現できるかを見る sanity / coverage benchmark である。詳細な module-to-claim 対応は \(\texttt{lean/PAPER\_MAPPING.md}\) に置き、本文では次の四型にまとめる。

第一は、乗法的・指数的 survival 型である。

\[
\begin{aligned}
\texttt{Survival.SerialReliability},\quad \\
\texttt{Survival.ConstantFractionDecay},\quad \\
\texttt{Survival.BranchingProcessExtinction},\quad \\
\texttt{Survival.BinarySymmetricChannel}
\end{aligned}
\]

がこれに入る。直列信頼性の \(\prod_{i} p_{i}\)、一定割合減衰の \(q^n\)、分岐過程の expectation-level \(m^n\)、binary channel の block success \((1-p)^n\) は、いずれも適切な一ステップ段階損失を置くと

\[
\exp(-L_{n})
\]

の形へ戻る。したがって、独立部分の積が対数段階損失の和へ変換されるという B3 型の加法性を、CSP 以外の 素朴な物理・工学・情報例で確認する層である。

第二は、線形過負荷型である。

\[
\begin{aligned}
\texttt{Survival.QueueStability},\quad \\
\texttt{Survival.MemoryThrashing}
\end{aligned}
\]

がこれに入る。ここでは対数比ではなく、到着率が処理率を超える backlog や、working set が物理メモリを 超える fault pressure が

\[
B_{n}=B_0+n(\lambda-\mu),\qquad P_{n}=P_0+n(W-M)
\]

のように線形に蓄積し、有限閾値を超える。これは「指数減衰」ではなく「資源超過の加法的蓄積」が崩壊を 引き起こす型である。

第三は、累積容量型である。

\[
\begin{aligned}
\texttt{Survival.FatigueDamage},\quad \\
\texttt{Survival.ConsensusFaultThreshold}
\end{aligned}
\]

がこれに入る。材料疲労の損傷 \(D_{n}=\sum_{i<n}d_{i}\) や分散合意の故障数 \(F_{n}=\sum_{i<n}f_{i}\) は、 非負量が有限容量または fault budget を超えると failure に入る。ここでは crack propagation の詳細や 特定の Byzantine agreement 定理ではなく、有限容量を超える累積量として collapse notion を切り出している。

第四は、臨界パラメータ型である。

\[
\begin{aligned}
\texttt{Survival.BucklingThreshold},\quad \\
\texttt{Survival.PercolationThreshold}
\end{aligned}
\]

がこれに入る。荷重 \(P_{n}=P_0+n\Delta P\) が臨界荷重 \(P_{\mathrm{cr}}\) に到達する例、占有率 \(p_{n}=p_0+n\Delta p\) が臨界占有率 \(p_{c}\) に到達する例は、安定/不安定、subcritical/supercritical の 切替を有限 prefix の閾値到達として扱う。Euler 座屈公式そのものや percolation の極限定理は、この層の 射程外である。

この整理により、仕様固定構造層の非CSP側は、指数型だけでなく、線形過負荷、累積容量、臨界パラメータという 三つのスカラー閾値型も含むことが明確になる。前者は B1/B3 の比率・対数段階損失の世界に直結し、後者は 集合値力学の collapse をスカラー累積量や制御パラメータの閾値到達として表した並列表現である。 この層の価値は「既知の事実を新定理として言い直すこと」ではなく、将来の operational theorem が どの型のドメインへ移植可能かを、有限 prefix の厳密な入口として先に固定しておく点にある。
\end{document}
