# Handoff Plan — 2026-04-22

Status: active handoff for the next LLM / agent.

Repository state at handoff:

```text
repo: /Users/sunagawa/Project/delta-survival-paper
branch: main
HEAD: run `git log --oneline -8` for exact current commits
remotes: origin/main and codeberg/main should be checked with `git status -sb`
```

The current program state includes the architecture pivot to the
structural persistence balance principle main theory, the M supplement reclassification, Route A
calibration-status notes, G6-c / G4 reader-facing supplements, Exp43c
q-coloring primary validation, the first frozen C-MAPSS FD001 cross-domain
loss-only primary, and the falsification / rival-framework stress-test memo.
Run `git log --oneline -8` for exact hashes.
Key commit subjects include:

```text
Add reader-facing G6-c Foster-Lyapunov supplement
Select G4 non-CSP anchors after G6-c closure
Add reader-facing G4 non-CSP anchor supplement
Draft threshold-local Route A redesign and Exp43b preregistration
Freeze Exp43c primary validation package
Record Exp43c primary validation pass
```

Early Exp43 q-coloring designs were exploration / pilot calibration, not
validation. Pilot_v1 and pilot_v2 are calibration data only. The original grid
should not be frozen or used for primary claims.

Exp43b q-coloring also remained exploration. Its threshold-local calibration
located usable informative windows, but `q=5,n=80,rho_fm=0.86` exceeded the
timeout gate (5/50 = 10%), so Exp43b is calibration no-go under the current
runtime rule. It is not a theory failure and not validation evidence. The
fresh successor draft is `analysis/exp43_qcoloring/exp43c_threshold_local_preregistration_draft.md`.

Exp43c q-coloring primary validation has passed under the frozen package. The
report is `analysis/exp43_qcoloring/exp43c_primary_report.md`. Main metric:
`fm_plus_n` mean held-out log loss `0.440189` vs best preregistered primary raw
baseline `2.804019` (84.3% improvement). H1 direction passed for all held-out
q folds, but q=5 was narrow (`0.426651` vs `0.438133`), so do not describe it
as a large q=5 effect.

Exp44 Cardinality-SAT is also exploratory calibration only. Smoke, pilot_v2,
and pilot_v3 were useful infrastructure / calibration checks but did not enter
validation.

Do not push without explicit user approval.

Use `git status` before continuing.

## 1. Current Research State

The internal controllable gates are now closed.

### Empirical Gates

Exp.40, Exp.41, Exp.42, and Mixed-CSP have all been preregistered, run,
analyzed, committed, pushed, and archived.

Key outcomes:

- **Exp.40**: scoped repair beats quality-blind contradiction baseline.
- **Exp.41**: width replication passed; `scoped > structural` held in both
  primary new models.
- **Exp.42**: scope gradient passed primary ordering; row-level analysis
  showed attribution-as-repair is the main mechanism.
- **Mixed-CSP**: official 12,000-row primary passed.
  - `L_plus_n` log loss: `0.0970`
  - `raw_plus_n` log loss: `0.7525`
  - `first_moment` log loss: `0.1489`
  - `cnf_count_plus_n` log loss: `0.1010`
  - relative improvement over `raw_plus_n`: `87.1%`
  - support criteria: primary, strong, theory-pure, encoding guardrail all pass.

Interpretation:

```text
This supports a Level 2 universality candidate across SAT / LLM / Mixed-CSP /
q-coloring. It is not yet a universal-law declaration; independent replication
remains the main external gate.
```

### Formal Gate

Lean M1 is complete.

M1 conclusion:

```text
Target theorem 4 is already formally accessible at the expectation level
through existing Lean theorems. M2-A mapping-only is sufficient.
```

Important distinction:

- expectation-level tendency is mapped to existing theorems;
- high-probability stopped-collapse remains a separate schema requiring
  concentration / margin assumptions;
- Bernoulli-CSP should be described as bad-event drift / Chernoff collapse
  tendency, not forced into repair-dominance vocabulary.

Main files:

- `lean/UNIVERSALITY_GAP_MAP.md`
- `lean/PAPER_MAPPING.md`
- `lean/TARGET_THEOREM4_M1_DESIGN.md`

G6-c iteration 1 is now closed at the minimal algebraic embedding level.

What is formalized:

- `analysis/g6c_formal_mapping_scope.md` fixes the scope boundary.
- `analysis/g6c_foster_lyapunov_embedding_draft.md` records the prose
  Foster-Lyapunov / queueing embedding and non-claims.
- `v2/補論_構造持続の収支原理とFoster-Lyapunovドリフトの形式的埋め込み.md`
  is the reader-facing supplement for the minimal embedding.
- `lean/Survival/LyapunovBalanceEmbedding.lean` proves:
  - `cumulativeAction_eq_load_diff` (\(B_n = Z_n-Z_0\));
  - `relativeMaintenance_succ_eq_mul_exp_neg_increment`
    (\(R_{t+1}=R_t e^{-b_t}\));
  - `increment_eq_lossFlow_sub_repairFlow` (\(b_t=\ell_t-g_t\));
  - queue wrappers against `QueueStability.excessDemand`.

What is not formalized:

- Foster-Lyapunov positive recurrence theorem;
- \(R_n \le R_0 e^{-cn}\) geometric ergodicity / exponential decay;
- continuous-time or non-Markov Lyapunov functional extensions.

Those belong to G6-c iteration 2.

G4 non-CSP anchor selection is recorded in
`analysis/g4_non_csp_anchor_selection.md`.
The reader-facing G4 v1 supplement is
`v2/補論_非CSP古典例における構造持続の収支原理の最小アンカー.md`.

Selected G4 v1 package:

- primary anchor: queueing / Foster-Lyapunov drift;
- loss-only control anchors: serial reliability and constant-fraction decay;
- secondary / coverage skeletons: branching expectation, fatigue, consensus,
  buckling, percolation.

G4 v2 direction is selected in
`analysis/g4_v2_repair_maintenance_scope.md` and integrated into the
reader-facing non-CSP supplement.

Selected G4 v2 scope:

- repair / maintenance reliability-fatigue balance;
- finite-prefix algebraic skeleton first, implemented in
  `lean/Survival/RepairMaintenanceBalance.lean`;
- reader-facing explanation in
  `v2/補論_非CSP古典例における構造持続の収支原理の最小アンカー.md` §11;
- operational pilot draft in
  `analysis/g4_v2_operational_pilot_preregistration_draft.md`;
- make compensation / repair flow \(g_t\) explicit in a non-CSP open-system
  anchor;
- defer branching-process almost-sure extinction to a later iteration.

## 2. Canonical Files To Read First

For a new agent, read these in order:

1. `analysis/universality_next_decisions.md`
   - current program status;
   - resolved gates;
   - recommended next options.
1. `README.md`
   - public-facing evidence status.
1. `analysis/current_evidence_map.md`
   - compact current map of core theory, Lean spine, Route A empirical anchors,
     Route C observational anchors, G4 non-CSP anchors, and open gates;
   - use this to avoid mixing validated support with exploration history.
1. `analysis/falsification_and_rival_frameworks.md`
   - stress-test memo for how the program could still be weakened or
     overturned after multiple anchors succeed;
   - main risks: sign inconsistency, non-preservation of theorem content,
     silent systems / scope overreach, rival-framework subsumption, and
     practical redundancy;
   - companion notes now exist at
     `analysis/ldp_rate_function_comparison.md` and
     `analysis/cross_domain_sign_convention_table.md`;
   - `analysis/scope_silence_catalog.md` is now the explicit F3 boundary memo.
1. `analysis/open_gates_execution_plan.md`
   - current execution order for the remaining open gaps:
     rival-framework defense -> conditional law-side bridge -> G7 replication
     package -> cross-domain non-CSP anchor -> repair-flow data acquisition.
1. `analysis/law_side_upgrade_gate.md`
   - defines the three conditions for a non-CSP domain to count as a
     conditional law-side bridge:
     natural \(m\), observable \(g_t\), explicit collapse / hitting boundary.
1. `analysis/non_csp_conditional_law_side_bridge.md`
   - applies that gate to queueing / Foster-Lyapunov, reliability controls,
     repair-maintenance, and the current observational anchors.
1. `analysis/g4_v2_repair_flow_acquisition_brief.md`
   - one-page practical packet for asking whether a partner / local /
     restricted dataset is structurally strong enough for repair-flow work.
1. `analysis/g4_v2_repair_flow_request_packet.md`
   - send-ready version of the acquisition brief;
   - asks for schema summary, rough counts, and field names before any raw-row
     transfer or dataset-specific freeze.
1. `analysis/g4_v2_stochastic_repair_bridge_note.md`
   - reader-facing theorem-side half-step for repair / maintenance:
     finite-prefix identity -> stopping / hitting bridge target.
1. `analysis/g4_v2_public_repair_flow_rescan_2026-04-27.md`
   - public-web rescan after the acquisition brief and bridge note;
   - Scania Component X is now the strongest current public stochastic
     reliability bridge candidate;
   - Azure PdM remains only a leakage-risk re-audit candidate.
1. `analysis/g4_scania_component_x_archive_feasibility_note.md`
   - exact public feasibility note for Scania Component X;
   - fixes current version identity at version 3 / DOI `10.5878/bnh5-ka77`;
   - keeps Scania below direct repair-flow primary and at stochastic bridge
     tier.
1. `analysis/g4_scania_component_x_large_readout_acquisition_note.md`
   - exact local acquisition of the three large public readout CSV files;
   - bytes, sha256 values, line counts, and repeated-unit counts fixed;
   - Scania public route is no longer blocked on archive identity.
1. `analysis/g4_scania_component_x_freeze_design_note.md`
   - locks the held-out five-class label grammar;
   - chooses horizon-classification as the operational primary path;
   - keeps survival / TTE as the conceptual law-side secondary path.
1. `analysis/g4_scania_component_x_horizon_bridge_preregistration_draft.md`
   - first operational prereg draft for the Scania public bridge route;
   - fixes the censored-as-class-0 rule, `D_pc1` compressed primary family,
     and multiclass log-loss target.
1. `analysis/g4_scania_component_x_horizon_bridge/freeze_manifest_draft.md`
   - frozen Scania bridge manifest;
   - records exact file identities, class grammar, model family, split roles,
     script SHA, and one-time primary command.
1. `analysis/g4_scania_component_x_horizon_bridge/validation_smoke_note.md`
   - first pre-freeze execution note for the Scania public bridge route;
   - metadata-only identity passes;
   - validation-smoke fits `B1/B2/B3/primary` without recording validation
     metrics or loading the held-out test surface.
1. `analysis/g4_scania_component_x_bridge_package_draft.md`
   - historical bridge-package draft after exact feasibility;
   - horizon classification is the operational primary path;
   - survival / TTE remains the conceptual law-side secondary path;
   - this design layer is now superseded by the frozen manifest and primary
     report.
1. `analysis/g4_scania_component_x_horizon_bridge/primary_report.md`
   - first frozen Scania public bridge result;
   - `H1 = false`, `H2 = true`, `H3 = true`, `primary_support = false`;
   - clean no-support outcome, not repair-flow evidence and not a same-archive
     rescue target.
1. `analysis/route_a_mixed_csp/mixed_csp_true_outside_rerun_01_katsumasa1234.md`
   - first returned Mixed-CSP true outside-group rerun;
   - WSL/Ubuntu 22.04.5 LTS, Python 3.10.12;
   - no reported workaround;
   - `12000` primary rows, `0` checked core mismatches, and all four support
     flags true.
1. `analysis/route_a_mixed_csp/mixed_csp_true_outside_rerun_02_SCRAPRO.md`
   - second returned Mixed-CSP true outside-group rerun;
   - Windows 11 Home, Python 3.12.7;
   - no reported workaround;
   - `12000` primary rows, `0` checked core mismatches, and all four support
     flags true.
1. `analysis/route_a_mixed_csp/mixed_csp_true_outside_interim_report.md`
   - interim G7 status for the Mixed-CSP outside-rerun request set;
   - `2/3` requested reruns completed;
   - `2/2` returned runs cleanly successful;
   - `1` requested rerun pending.
1. `analysis/g7_mixed_csp_replication_package_plan.md`
   - first concrete G7 runbook;
   - Mixed-CSP was the recommended first independent replication target before
     Exp43c or Backblaze v2, and now has two clean returned outside-group runs.
1. `analysis/route_a_mixed_csp/mixed_csp_audit_replay_note.md`
   - Level 1 audit replay is complete;
   - official JSONL / JSON / summary are internally consistent;
   - this note is Level 1 only; see the separate Level 2 rerun note below.
1. `analysis/route_a_mixed_csp/mixed_csp_level2_rerun_note.md`
   - Level 2 local fresh rerun is complete on separate outputs;
   - core non-runtime fields reproduce the official JSONL exactly;
   - true outside-group rerun is now underway, with the first returned run
     recorded separately.
1. `analysis/route_a_mixed_csp/mixed_csp_external_rerun_package.md`
   - external rerun bundle boundary for Mixed-CSP;
   - lists the minimum files, hashes, and quickstart commands for an outside
     replicator.
1. `analysis/route_a_mixed_csp/mixed_csp_outside_workspace_rerun_note.md`
   - fresh-clone rehearsal of the external Mixed-CSP handoff;
   - separate outputs, same frozen commit, checked core-field exact match;
   - stronger than a main-worktree rerun, but still not an outside-group
     replication.
1. `analysis/route_a_mixed_csp/mixed_csp_published_remote_rerun_note.md`
   - rerun from a fresh clone of the actually published remote at commit
     `96de727`;
   - separate outputs, checked core-field exact match, and support-flag match;
   - stronger than the earlier rehearsal, still not outside-group replication.
1. `analysis/route_a_mixed_csp/mixed_csp_true_outside_handoff_checklist.md`
   - final send checklist for a real outside-group rerun;
   - package identity, before-send checks, exact return bundle, and success /
     failure criteria.
1. `analysis/route_a_mixed_csp/mixed_csp_true_outside_send_runbook.md`
   - sender-side operational runbook for the actual external send;
   - exact-hash capture, attachment list, and return-bundle flow.
1. `analysis/g7_true_outside_handoff_overview.md`
   - one-page ordering note for the actual external send sequence;
   - absorb the pending Mixed-CSP returns while sending the Exp43c
     outside-group zip packet.
1. `analysis/exp43_qcoloring/exp43c_published_remote_rerun_note.md`
   - rerun from a fresh clone of the actually published remote at commit
     `96de727`;
   - exact manifest/evaluation match and checked core-field exact match;
   - stronger than the local Level 2 rerun alone, still not outside-group
     replication.
1. `analysis/exp43_qcoloring/exp43c_true_outside_handoff_checklist.md`
   - final send checklist for a real outside-group rerun;
   - package identity, before-send checks, exact return bundle, and success /
     failure criteria.
1. `analysis/exp43_qcoloring/exp43c_true_outside_send_runbook.md`
   - sender-side operational runbook for Exp43c true outside-group handoff;
   - exact-hash capture, zip fallback, and return-bundle flow.
1. `analysis/exp43_qcoloring/exp43c_true_outside_send_packet_ja.md`
   - Japanese send-ready request text for the Exp43c outside rerun.
1. `analysis/exp43_qcoloring/exp43c_zip_receiver_guide_ja.md`
   - zip recipient guide using one-line `python ...` commands to avoid
     Windows argument-passing ambiguity.
1. `analysis/exp43_qcoloring/exp43c_execution_environment_note_template_ja.md`
   - environment memo template for outside runners.
1. `analysis/exp43_qcoloring/exp43c_g7_replication_report_template.md`
   - report template to fill after a returned outside-group Exp43c rerun.
1. `analysis/exp43_qcoloring/handoff_exports/LOCKED_BUNDLE_NOTE.md`
   - locked Exp43c distribution bundle identity;
   - records current distribution bundle
     `exp43c_true_outside_bundle_1f05ff13211a.zip` and sha256;
   - supersedes the older `40517dfb4b61` bundle.
1. `analysis/exp43_qcoloring/handoff_exports/SENT_ARCHIVE_NOTE.md`
   - records that `exp43c_true_outside_bundle_1f05ff13211a.zip` has been sent;
   - points to the ignored local archive copy and states not to overwrite the
     sent bundle in place.
1. `analysis/exp43_qcoloring/handoff_exports/依頼文.md`
   - filled Japanese request text for the locked Exp43c zip bundle.
1. `analysis/exp43_qcoloring/handoff_exports/返信が来たらやること.md`
   - return-processing checklist for Exp43c outside rerun artifacts.
1. `analysis/g4_v2_exploratory_dataset_scan.md`
   - exploration-only scan plan for maintenance / repair / incident /
     degradation datasets;
   - use this before dataset ranking or freeze;
   - it allows schema inspection and feasibility notes, but no validation
     evidence or theory-support claims.
1. `analysis/g4_cmapss_loss_only_feasibility_note.md`
   - current cross-domain non-CSP loss-only candidate note;
   - feasible as a public loss-only anchor, explicitly not repair-flow.
1. `analysis/g4_cmapss_fd001_archive_note.md`
   - exact first C-MAPSS subset choice note;
   - first subset is `FD001` inside the standard `CMAPSSData.zip` family.
1. `analysis/g4_cmapss_fd001_archive_feasibility_note.md`
   - exact C-MAPSS archive acquisition note;
   - records a concrete `CMAPSSData.zip` hash and FD001 structural counts.
1. `analysis/g4_cmapss_fd001_loss_only_preregistration_draft.md`
   - first freeze-eligible C-MAPSS loss-only draft path;
   - binary near-failure event with `H = 50 cycles`, fixed FD001 archive,
     PCA-based low-dimensional degradation coordinate, and wide-sensor
     compression guardrail.
1. `analysis/g4_cmapss_fd001_loss_only/freeze_manifest_draft.md`
   - frozen package for the first C-MAPSS FD001 loss-only run;
   - archive, parser, eligible sensor set, PCA orientation rule, model class,
     and script hash are fixed.
1. `analysis/g4_cmapss_fd001_loss_only/train_smoke_note.md`
   - no-peek parser / train-fit check for the FD001 execution script;
   - exact archive hash matches the feasibility note;
   - all preregistered train-side fits succeed without any held-out metrics.
1. `analysis/g4_cmapss_fd001_loss_only/primary_report.md`
   - frozen held-out FD001 primary result;
   - weakening outcome only: `H1/H3 pass`, `H2 fail`, `primary_support = false`;
   - useful low-dimensional loss-only signal, but weaker than the wide
     raw-sensor benchmark.
1. `analysis/g4_v2_repair_flow_candidate_criteria.md`
   - explicit gate for any future repair-flow dataset;
   - use this before ranking any public or private maintenance-log candidate.
1. `analysis/g4_v2_operational_pilot_preregistration_draft.md`
   - draft-only operational pilot for maintenance / repair logs;
   - tests whether lagged repair-aware predictors beat damage-only and
     activity-only baselines;
   - no dataset is frozen, and no validation data should be generated yet.
1. `analysis/structural_persistence_balance_principle_draft_plan.md`
   - current architecture pivot toward structural persistence balance principle / 構造持続の収支原理;
   - treats M decomposition as an operational mapping layer, not the universal
     core.
1. `v2/3_構造持続の収支原理と崩壊傾向.md`
   - new main-theory draft slot for structural persistence balance principle;
   - §1-8 currently establish the position, minimal balance identity, local
     balance law, signed exponential kernel, expectation-level tendency,
     concentration / hitting-time schema, Route A anchors, Route C anchors, and
     existing-theory differences, then close with limitations / next steps.
1. `v2/data/ROADMAP_構造持続の収支原理とRouteA強化.md`
   - step-by-step roadmap for another LLM;
   - records the order: historical balance-principle Paper 5 (now main Paper 3),
     Lean theorem map, Route A
     calibration discipline, G6-c formal mapping, and non-CSP / replication
     gates.
1. `v2/data/exp43_qcoloring_preregistration_draft.md`
   - draft-only Exp43 q-coloring cross-q feasibility preregistration;
   - tests Route A width beyond SAT/NAE Mixed-CSP;
   - historical draft / harness lineage; Exp43c is the fresh threshold-local
   validated successor;
   - `v2/data/` is gitignored, so force-add if freezing it in history.
1. `analysis/exp43_qcoloring/`
   - draft pilot harness for Exp43;
   - includes deterministic generator, q-coloring CNF encoder, optional PySAT
     solver wrapper, feature extractor, runner, summary, config, and tests;
   - smoke, pilot_v1, pilot_v2, Exp43b calibration, Exp43c calibration, and
     Exp43c primary have run locally;
   - `analysis/exp43_qcoloring/phase_status.md` records the sequence from
     exploration pilots to the Exp43c primary validation;
   - `analysis/exp43_qcoloring/pilot_v1_addendum.md` records the pilot_v1
     fallback decision;
   - `analysis/exp43_qcoloring/pilot_v2_summary.md` records that pilot_v2 did
     not pass the freeze gate;
   - current primary report: `analysis/exp43_qcoloring/exp43c_primary_report.md`.
   - current local rerun note:
     `analysis/exp43_qcoloring/exp43c_level2_rerun_note.md`.
1. `analysis/exp43_qcoloring/exp43c_external_rerun_package.md`
   - outside handoff note for Exp43c;
   - uses `replication_outputs/` and separate manifest / results /
     evaluation outputs to avoid touching official artifacts.
1. `analysis/exp44_cardinality_sat/preregistration_draft.md`
   - draft-only Exp44 Cardinality-SAT Route A width-extension design;
   - uses heterogeneous `AL1_4` / `EX2_4` / `EX1_4` cardinality constraints;
   - primary candidate predictor is `first_moment_log_count = n log 2 - L`;
   - not frozen, and no Exp44 primary data should be generated yet.
1. `analysis/exp44_cardinality_sat/`
   - Exp44 pilot harness;
   - includes deterministic generator, direct forbidden-pattern CNF encoder,
     semantic verifier, optional PySAT solver wrapper, feature extractor,
     runner, summary, config, and tests;
   - smoke has run locally and is recorded in `smoke_summary.md`;
   - a partial pilot runtime probe is recorded in `pilot_runtime_probe.md`;
   - pilot_v2 completed and is recorded in `pilot_v2_summary.md`;
   - pilot_v3 completed and is recorded in `pilot_v3_summary.md`;
   - current Exp44 calibration is not freeze-ready;
   - primary has not run.
1. `v2/補論_構造持続における資源項Mの操作的定式化.md`
   - supplement-level integrated M operationalization draft;
   - do not promote this to main `v2/3_...md` unless the architecture decision
     changes.
1. `v2/補論_有限CSPにおける構造持続の予測力.md`
   - Mixed-CSP official result and interpretation.
1. `lean/PAPER_MAPPING.md`
   - formal theorem map.
1. `lean/UNIVERSALITY_GAP_MAP.md`
   - M1 decisions for G1-G5.
1. `v2/data/paper5/PAPER5_DRAFT_PLAN.md`
   - historical Paper 5 review draft plan (for the current Paper 3 lineage);
   - use this as source material for the M supplement, not as an active main
     preprint promotion plan;
   - §12 records provisional decisions (D1 two-level `F`, D2 representation
     discipline, D3 validation protocol, D4 mode/channel split, D5 software
     Route C mapping, D6 DeltaLint split, D7 validation protocol, D8 closure,
     D9 promotion summary / Lean non-blocker).
1. `v2/data/paper5/PAPER5_SECTION_1_2_DRAFT.md`
   - §1-2 main-text review draft (problem setting and minimal form);
   - §2.2 separates internal persistence modes from external supply channels;
   - §2.5 treats `Phi` as representation discipline, naming product / CES /
     bottleneck candidate families rather than a load-bearing product theorem;
   - do not promote to `v2/3_...md` before user review.
1. `v2/data/paper5/PAPER5_SECTION_3_DRAFT.md`
   - §3 main-text review draft (Route C companion I / II mode mapping);
   - introduces in-context `M_r` vs `M_x`-supplied `M_r` as shorthand for
     `M_{x->r}`;
   - treats Paper 3/4 values as indicators, not direct mode estimates.
1. `v2/data/paper5/PAPER5_SECTION_4_5_DRAFT.md`
   - §4-5 main-text review draft (software / SaaS mapping and intervention-ranking prediction);
   - fixes software as Route C, broad `F` as safe change continuity, and pilot
     `F` as change-introduced bug detection / localization;
   - treats prompt/runbook/checklist/CI-CD/protocol as `Sigma`, not raw `R`.
1. `v2/data/paper5/PAPER5_SECTION_6_DRAFT.md`
   - §6 main-text review draft (empirical validation protocol);
   - fixes time-split primary and leave-one-project-out secondary;
   - separates risk-prediction support from intervention-ranking support;
   - requires rho_i, Phi, and A_j robustness checks;
   - treats observational ranking support as non-causal without explicit
     causal identification.
1. `v2/data/paper5/PAPER5_SECTION_7_DRAFT.md`
   - §7 main-text review draft (limitations and future work);
   - closes the M draft as a framework / protocol draft, not an empirical-pilot
     completion;
   - keeps four-domain comparison and dynamic collapse profiles as future work.
1. `v2/data/paper5/DELTALINT_PAPER3_EXTENSION_NOTE.md`
   - separates DeltaLint from the M supplement;
   - treats DeltaLint as L-side / Paper 3 static-code extension candidate;
   - records additive-prediction protocol: existing tools + DeltaLint >
     existing tools alone.
1. `v2/data/paper5/PAPER5_REVIEW_SUMMARY.md`
   - promotion summary for §1-7;
   - fixes the essence: support-side coordinate system, not a universality
     proof;
   - states Lean formalization is optional, not a current blocker.
1. `v2/data/paper5/paper5_メモ.md`
   - raw notes for the M supplement.
1. `v2/data/チャットメモ/chatgpt_2026-04-22_ソフトウェア.md`
   - software / operationalization memo.

## 3. What Not To Do

Do not make these mistakes:

- Do not say universality is fully established.
- Do not claim a universal constant or drift-to-`c` point prediction.
- Do not merge expectation-level tendency with high-probability stopped-collapse.
- Do not force Bernoulli-CSP into repair-dominance language.
- Do not promote XOR-SAT as a primary empirical Route A anchor.
- Do not treat LDPC decoder performance as implied by drift-weighted `L` alone.
- Do not call bootstrap percolation Route A without changing the protocol.
- Do not push / OSF-upload new work without explicit user approval.

## 4. Recommended Next Work

The strongest next task has changed. Do **not** promote M decomposition as the
next main preprint. The user decided that M decomposition is supplement-level: it is an
operational map for measuring compensation / resource flow, not the universal
core.

Current architecture:

```text
Universal core candidate:
  structural persistence balance principle / 構造持続の収支原理

Supplement-level operational layer:
  M mode decomposition and intervention-ranking protocol
```

The active new draft plan is:

```text
analysis/structural_persistence_balance_principle_draft_plan.md
```

The integrated M draft now lives at:

```text
v2/補論_構造持続における資源項Mの操作的定式化.md
```

The structural persistence balance principle main draft now starts at:

```text
v2/3_構造持続の収支原理と崩壊傾向.md
```

Historical Paper 5 review-draft files remain useful as source material for the
current Paper 3 lineage:

```text
v2/data/paper5/PAPER5_DRAFT_PLAN.md            (control document, with §12 decisions)
v2/data/paper5/PAPER5_SECTION_1_2_DRAFT.md     (§1-2 main-text review draft)
v2/data/paper5/PAPER5_SECTION_3_DRAFT.md       (§3 Paper 3/4 mode mapping draft)
v2/data/paper5/PAPER5_SECTION_4_5_DRAFT.md     (§4-5 software mapping + intervention ranking)
v2/data/paper5/PAPER5_SECTION_6_DRAFT.md       (§6 empirical validation protocol)
v2/data/paper5/PAPER5_SECTION_7_DRAFT.md       (§7 limitations + future work)
v2/data/paper5/PAPER5_REVIEW_SUMMARY.md        (§1-7 promotion review summary)
```

Use these files as traceable source drafts for the supplement. They are no
longer the active route to a main `v2/3_...md` preprint. All are local-only; do
not push.

Purpose:

```text
Review and polish the structural persistence balance principle as the next main-theory candidate:
loss flow, compensation flow, one-step balance, expectation-level tendency,
finite-horizon concentration, and hitting-time bounds.
```

Default next action:

```text
G6-c iteration 1 is closed. Do not expand it into positive recurrence or
geometric ergodicity unless explicitly starting G6-c iteration 2. The next
highest-value choices are:

1. G4 v2 exploration-only dataset scan, using
   `analysis/g4_v2_exploratory_dataset_scan.md`;
2. theorem-map cleanup for the selected G4 v1 / v2 package, if needed;
3. independent replication / external review workstream.
```

Do not create or promote `v2/3_構造持続における資源項Mの操作的定式化.md`
under the current architecture. The numbered main-theory draft is now the
structural persistence balance principle paper, not the M decomposition paper.

Provisional decisions already recorded in `PAPER5_DRAFT_PLAN.md` §12:

- D1 two-level `F`: broad `safe change continuity` + narrow pilot `bug detection / localization`;
- D2 representation discipline: §2.5 names product / CES / bottleneck
  candidate families, but does not make product form load-bearing;
- D3 validation protocol: time-split primary + leave-one-project-out secondary, both preregistered;
- D4 mode vs supply channel distinction: in-context `M_r` vs `M_x`-supplied
  `M_r` (formal notation `M_{x->r}`);
- D5 software Route C mapping: prompt/runbook/checklist/CI-CD/protocol belong to `Sigma`, and `L_hat_pilot` starts from boundary-crossing count + rollback-impossibility rate.
- D6 DeltaLint split: DeltaLint is not the M supplement's load-bearing validation; treat
  it as a separate L-side / Paper 3 static-code extension.
- D7 §6 validation protocol: time-split primary, leave-one-project-out
  secondary, risk-prediction support vs intervention-ranking support, rho_i /
  Phi / A_j robustness checks, strong scalar `M_total` baseline, preregistered
  `rank_obs` estimator, minimum unit / power target, and observational-vs-causal
  caveat.
- D8 §7 closure: the M draft is a framework / protocol draft, not an empirical-pilot
  completion; four-domain comparison and dynamic collapse profiles are future
  work.
- D9 promotion summary / Lean non-blocker: the M draft is a support-side coordinate
  system for intervention ranking, not a universality proof; Lean formalization
  is optional and not a promotion gate.

Active main-preprint draft:

```text
v2/3_構造持続の収支原理と崩壊傾向.md
```

For M-supplement cleanup, preserve this essence:

```text
- The M draft is thin and support-side: mode decomposition + intervention-ranking
  protocol.
- no completed M-side empirical pilot yet;
- software is Route C, not Route A;
- L_hat_pilot is a proxy, not true L;
- intervention-ranking support requires intervention history;
- observational intervention-ranking support is not causal proof without
  randomization or explicit causal identification;
- underpowered pilots should not be read as strong non-support;
- risk prediction alone is only preparatory support;
- rho_i / Phi / A_j choices require robustness checks;
- DeltaLint is separate L-side work, not M-supplement validation.
```

DeltaLint is split out. The starter note is
`v2/data/paper5/DELTALINT_PAPER3_EXTENSION_NOTE.md`; it uses the additive
prediction protocol `existing tools + DeltaLint > existing tools alone` under
the same alert budget.

## 5. Optional Work, Lower Priority

### M2-B Wrapper

Only do this if paper writing needs named Lean theorem aliases.

Possible file:

```text
lean/Survival/TargetTheorem4Mapping.lean
```

Candidate wrappers:

- generic expectation wrapper;
- coarse expectation wrapper;
- SAT target theorem 4 wrapper.

Requirement:

```text
If any Lean code is edited, run `cd lean && lake build`.
```

### Route A Width Extensions

Exp43c q-coloring has now passed primary validation under the frozen
threshold-local package. Treat it as Route A width support beyond SAT syntax,
not as a universal-law declaration.

Next choices:

- Exp43c is now integrated into the Route A extension map / finite-CSP
  supplement;
- optionally polish robustness reporting around the narrow q=5 fold;
- Exp44: keep Cardinality-SAT as a later Route A width experiment, not a
  required immediate gate.

Do not treat Cardinality-SAT as a required gate. It is a reach / width
extension if q-coloring calibration is closed as inconclusive.

The current Exp44 draft lives at:

```text
analysis/exp44_cardinality_sat/preregistration_draft.md
```

The Exp44 harness and smoke summary live at:

```text
analysis/exp44_cardinality_sat/
analysis/exp44_cardinality_sat/smoke_summary.md
```

Smoke, pilot_v2, and pilot_v3 have passed infrastructure checks. Pilot_v3 did
not pass the freeze gate because M0/M1/M2 each still had only one informative
rho band. The current Exp44 calibration design should be marked inconclusive /
not freeze-ready. Do not generate Exp44 primary data from the current design.

### G6-c Follow-up

G6-c iteration 1 is closed as a minimal Lean-backed algebraic embedding. If
continuing G6-c, start a new iteration with an explicit scope lock. Do not
quietly turn the current draft into a proof of positive recurrence or
geometric ergodicity.

Allowed next G6-c work:

- supplement polish / reader-facing exposition, if needed after review;
- theorem-map cleanup;
- choosing a G4 non-CSP anchor (queueing, reliability, branching, population
  dynamics) using the embedding as a filter.

Deferred to G6-c iteration 2:

- Foster-Lyapunov positive recurrence theorem;
- \(R_n \le R_0 e^{-cn}\) geometric ergodicity / exponential decay;
- continuous-time generator / stochastic thermodynamics;
- non-Markov Lyapunov functional generalization.

## 6. Suggested Next-Session Prompt

If handing to another LLM, use:

```text
We are in /Users/sunagawa/Project/delta-survival-paper.
Read analysis/HANDOFF_2026-04-22.md first.
Do not push without explicit approval.
Exp43 q-coloring v1/v2 were exploration / pilot calibration, not validation.
The original grid did not pass the freeze gate and should not be used for
primary claims.
Exp43b threshold-local calibration also remained exploration. It located
informative windows, but failed the runtime gate at q=5,n=80,rho_fm=0.86
(5/50 timeouts). Treat Exp43b as calibration no-go, not validation evidence.
Exp43c replaced Exp43b as a fresh preregistration, passed calibration, was
frozen, and then passed primary validation. Report:
`analysis/exp43_qcoloring/exp43c_primary_report.md`.
Exp44 Cardinality-SAT is also exploration / pilot calibration only. Its
current calibration design did not pass the freeze gate, so primary Exp44 data
must not be generated from it.
G6-c iteration 1 is closed as a minimal Foster-Lyapunov / queueing algebraic
embedding. Do not claim positive recurrence or geometric ergodicity from it.
If continuing G6-c, start iteration 2 explicitly.
Use the historical Paper 5 review drafts only as M-side source material, not as
a main
M-decomposition paper.
Control document: analysis/structural_persistence_balance_principle_draft_plan.md.
Current structural persistence balance principle draft:
- v2/3_構造持続の収支原理と崩壊傾向.md
M operationalization supplement:
- v2/補論_構造持続における資源項Mの操作的定式化.md
Roadmap:
- v2/data/ROADMAP_構造持続の収支原理とRouteA強化.md
Exp43 q-coloring draft:
- v2/data/exp43_qcoloring_preregistration_draft.md
Exp43b threshold-local redesign:
- analysis/protocols/route_a_threshold_local_redesign_note.md
- analysis/exp43_qcoloring/exp43b_threshold_local_preregistration_draft.md
- analysis/exp43_qcoloring/exp43b_calibration_closeout.md
Exp43c threshold-local fresh preregistration:
- analysis/exp43_qcoloring/exp43c_threshold_local_preregistration_draft.md
- analysis/exp43_qcoloring/exp43c_calibration_closeout.md
- analysis/exp43_qcoloring/exp43c_freeze_package.md
- analysis/exp43_qcoloring/exp43c_primary_report.md
Exp43 pilot harness:
- analysis/exp43_qcoloring/
Exp43 phase status:
- analysis/exp43_qcoloring/phase_status.md
Exp44 Cardinality-SAT draft / harness:
- analysis/exp44_cardinality_sat/preregistration_draft.md
- analysis/exp44_cardinality_sat/smoke_summary.md
- analysis/exp44_cardinality_sat/pilot_runtime_probe.md
- analysis/exp44_cardinality_sat/pilot_v2_summary.md
- analysis/exp44_cardinality_sat/pilot_v3_summary.md
G6-c formal mapping:
- analysis/g6c_formal_mapping_scope.md
- analysis/g6c_foster_lyapunov_embedding_draft.md
- v2/補論_構造持続の収支原理とFoster-Lyapunovドリフトの形式的埋め込み.md
- lean/Survival/LyapunovBalanceEmbedding.lean
G4 non-CSP anchor selection:
- analysis/g4_non_csp_anchor_selection.md
- v2/補論_非CSP古典例における構造持続の収支原理の最小アンカー.md
G4 / Backblaze loss-only observational branch:
- analysis/g4_v2_exploratory_dataset_scan.md
- analysis/g4_c1_azure_pdm_schema_inspection_note.md
- analysis/g4_c2_metropt3_schema_inspection_note.md
- analysis/g4_c3_backblaze_loss_only_schema_inspection_note.md
- analysis/g4_backblaze_loss_only_preregistration_draft.md
- analysis/g4_backblaze_q4_2025_archive_feasibility_note.md
- analysis/backblaze_loss_only/freeze_manifest_draft.md
- analysis/backblaze_loss_only/validation_smoke_note.md
- analysis/backblaze_loss_only/primary_report.md
Historical M-source drafts:
- v2/data/paper5/PAPER5_DRAFT_PLAN.md
- v2/data/paper5/PAPER5_SECTION_1_2_DRAFT.md
- v2/data/paper5/PAPER5_SECTION_3_DRAFT.md
- v2/data/paper5/PAPER5_SECTION_4_5_DRAFT.md
- v2/data/paper5/PAPER5_SECTION_6_DRAFT.md
- v2/data/paper5/PAPER5_SECTION_7_DRAFT.md
- v2/data/paper5/PAPER5_REVIEW_SUMMARY.md
Use the M draft as support-side operational mapping for compensation/resource
flow, not as the universal-law core. The core candidate is 構造持続の収支原理:
b_t = ell_t - g_t, B_n = sum b_t, m(V_n)=m(V_0)exp(-B_n), plus expectation
tendency and finite-horizon concentration / hitting-time bounds.
Draft conservatively: no universal-law declaration beyond stated assumptions,
no new empirical claim, and keep high-probability bounds separate from
expectation-level tendency.
```

## 7. Current Closure Statement

As of the G6-c iteration 1 closure documentation edits,
the program is internally synchronized:

```text
empirical gates: closed for current internal program;
formal expectation-level gate: closed via M1 mapping;
G6-c iteration 1: closed as minimal Lean-backed algebraic embedding;
public docs: synchronized;
G4 anchor selection: queueing / Foster-Lyapunov primary, serial reliability and
constant-fraction decay as loss-only controls;
G4 v1 supplement: complete as reader-facing minimal anchor package;
G4 v2 scope: selected as repair / maintenance reliability-fatigue balance, with
finite-prefix algebraic skeleton implemented in
`RepairMaintenanceBalance.lean` and integrated into the G4 reader-facing
supplement;
G4 repair-flow public dataset search: paused after C1 Azure PdM (leakage-risk)
and C2 MetroPT-3 (weak-g);
Backblaze loss-only observational branch: frozen and run on Q4 2025; primary
support failed under the preregistered log-loss metric (`1.779176` vs best
baseline `0.157102`) and H2 sign consistency failed due `smart_199_raw`;
Backblaze same-domain retry boundary fixed in
`analysis/backblaze_loss_only_v2_exploration_note.md`:
Q4 2025 stays closed, v2 is allowed only on a fresh untouched archive under a
new preregistration / freeze;
metadata-only archive ranking for a same-domain retry fixed in
`analysis/backblaze_loss_only_v2_archive_ranking_note.md`:
Q3 2025 first, then Q2 2025, then Q1 2025; next action is Q3 2025 feasibility
inspection only, with no predictor-level inspection;
Q3 2025 feasibility inspection completed in
`analysis/g4_backblaze_q3_2025_archive_feasibility_note.md`:
fresh archive candidate is structurally feasible, all required SMART / metadata
fields are present, and the H=30 endpoint horizon has 571 raw failure rows;
Backblaze v2 same-domain retry draft opened in
`analysis/backblaze_loss_only_v2_preregistration_draft.md`:
Q3 2025 fixed as the fresh archive, calibration-aware path precommitted,
`smart_199_raw` excluded from the primary theory-side loss vector, not yet
frozen and not validation evidence;
Backblaze v2 freeze-manifest candidate opened in
`analysis/backblaze_loss_only_v2/freeze_manifest_draft.md`:
Q3 archive and split fixed, stage-1 / stage-2 calibration path specified,
script hash intentionally left blank until the v2 evaluation script exists and
passes metadata-only / validation-smoke modes;
Backblaze v2 implementation now exists in
`analysis/backblaze_loss_only_v2/scripts/evaluate_backblaze_loss_only_v2.py`;
Backblaze v2 validation-smoke completed in
`analysis/backblaze_loss_only_v2/validation_smoke_note.md`:
full Q3 train / validation scale completed, test block untouched, not
validation evidence;
Backblaze v2 manifest is now frozen in
`analysis/backblaze_loss_only_v2/freeze_manifest_draft.md`:
final script SHA fixed, package frozen, next clean move is one Q3
`--allow-primary-run` execution only;
Backblaze v2 primary is now completed and recorded in
`analysis/backblaze_loss_only_v2/primary_report.md`:
Q3 2025 calibration-aware same-domain retry passed under the frozen decision
rule (`0.007936` vs best baseline `0.008801`, 9.83% improvement), H2 passed on
the five core SMART fields, and H3 passed (`0.882895` > `0.739014`); this is
same-domain observational loss-only support only and does not erase the Q4
2025 no-support result;
C-MAPSS FD001 freeze package is now frozen in
`analysis/g4_cmapss_fd001_loss_only/freeze_manifest_draft.md`:
archive hash, parser, nonconstant sensor set, PCA orientation rule, model
class, and script hash are fixed;
C-MAPSS FD001 primary is now completed and recorded in
`analysis/g4_cmapss_fd001_loss_only/primary_report.md`:
the first frozen cross-domain loss-only run produced the preregistered
weakening outcome (`H1 = true`, `H2 = false`, `H3 = true`,
`primary_support = false`); `D_pc1 + cycle + settings` beat the best simple
baseline (`0.241353` vs `0.494974`, 51.24% improvement) but remained weaker
than the wide raw-sensor model `B4 = 0.182180`; this is not repair-flow
evidence and not full cross-domain non-CSP support;
remaining central work: theorem-map cleanup if needed, no post-hoc rescue of
Backblaze or same-archive C-MAPSS, threshold-local Route A redesign only if
explicitly labeled exploration, G6-c iteration 2 only if positive recurrence /
geometric ergodicity is explicitly in scope, absorb the one pending Mixed-CSP
outside-group return, send the Exp43c outside-group zip packet and record the
first return, Scania-style public stochastic bridge follow-up only if pursued,
and optional expansion.
```
