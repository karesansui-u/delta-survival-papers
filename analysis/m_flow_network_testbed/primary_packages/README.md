# M Flow Network Primary Packages

Status: pre-primary package records.

Primary packages freeze candidate configs, seed lists, command records, and
hashes before any outcome-bearing execution.  They are not support evidence.

Current package:

- `final_candidate_v0/`: Q / damage / horizon package selected after
  calibration review v1.
