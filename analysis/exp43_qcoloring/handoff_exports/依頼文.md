# Exp43c 依頼文

この文面は `analysis/exp43_qcoloring/exp43c_true_outside_send_packet_ja.md`
の送信用コピーです。送る前に次を埋める。

```text
<SEND_COMMIT_HASH>
<ZIP_FILENAME>
<ZIP_SHA256>
```

## 短文

はじめまして。

研究用 Python パッケージの再現実行について、ご相談させていただきました。

お願いしたいのはコード修正や改善ではなく、こちらで用意した zip ファイルと
手順書を使って、ご自身の環境で凍結済みの q-coloring 実験をそのまま再実行
し、結果ファイルを返送していただく作業です。

zip 名:

```text
<ZIP_FILENAME>
```

zip sha256:

```text
<ZIP_SHA256>
```

元になった公開 commit:

```text
<SEND_COMMIT_HASH>
```

可能でしたら、手順書どおりに実行し、出力ファイル、ハッシュ、行数、簡単な
実行環境メモをご返送いただけますと幸いです。

## 長文

【Python実行のご依頼】研究用 q-coloring パッケージの再現実行
（zip解凍・Python実行・改善不要）

zip でお渡しする Python 実行パッケージを、手順書どおりにご自身の PC 環境
で実行し、結果ファイルを返送していただきたいです。

新機能開発や改善提案ではなく、凍結済み実験の再現実行作業になります。

【依頼内容】

作業内容:

1. zip ファイルを受け取る
2. zip を展開する
3. Python 依存関係を導入する
4. 手順書どおりに事前確認と本実行を行う
5. 指定された出力ファイルと実行メモを返送する

やってほしいこと:

1. 手順書どおりに実行する
2. 出力を指定ディレクトリに保存する
3. 結果ファイル、ハッシュ、行数、ステータス数、簡単な実行メモを返送する

やらなくてよいこと:

1. コード修正
2. 改善提案
3. 評価指標の変更
4. 設計変更
5. 新機能開発

使用言語:

```text
Python
```

実行形式:

```text
コマンドライン実行
```

注意:

1. AI を手順整理の補助に使っていただいて構いません
2. ただし、推測でコードや条件を変更しないでください
3. うまくいかない場合は、変更せずにエラーメッセージをそのまま共有してください

zip 名:

```text
<ZIP_FILENAME>
```

zip sha256:

```text
<ZIP_SHA256>
```

元になった公開 commit:

```text
<SEND_COMMIT_HASH>
```

ご確認お願いいたします。
