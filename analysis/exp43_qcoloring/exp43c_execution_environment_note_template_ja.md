# 実行環境メモ テンプレート（Exp43c）

このファイルをコピーするか、そのまま編集して返送してください。

## 1. 実行者

```text
実行者名または識別名:
実行日:
```

## 2. 実行環境

```text
OS:
Python version:
仮想環境を使ったか:
pip install -r requirements.txt は成功したか:
```

分かる範囲で大丈夫です。

```text
python-sat / pysat version:
numpy version:
scikit-learn version:
scipy version:
```

## 3. 実行結果

```text
手順書どおりに最後まで実行できたか:
途中でエラーが出たか:
回避策を使ったか:
```

回避策を使った場合は、何をしたかを書いてください。

```text
回避策の内容:
```

## 4. 返送ファイル

```text
exp43c_primary_manifest_external.jsonl を返送したか:
exp43c_primary_results_external.jsonl を返送したか:
exp43c_primary_evaluation_external.json を返送したか:
```

## 5. 一言の結論

記入欄:

```text

```
