A06-stop Coding-Channel Stopping-Set Recovery
=============================================

This directory contains pre-freeze smoke tooling for the
`coding_channel_stopping_set_recovery` candidate.

Status: smoke tooling only; not frozen; not validation evidence.

Current intended checks:

- counter feasibility micro-benchmark;
- BEC peeling decoder audit;
- all-stopping-set counter sanity through the requested order;
- endpoint prevalence diagnostics;
- guardrail plumbing for hazard and rank-dependency baselines.

The smoke harness must not be interpreted as support or no-support.
